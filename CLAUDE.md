@AGENTS.md

## ⚠️ 关键排障 / 会话交接文档索引（新 AI 首读）

> **本项目由三仓 fork 组成，只单向同步上游 → fork，永不反向提 PR。** 三仓对上游的映射、版本对照、当前同步状态、同步套路见：[`docs/guides/upstream-sync-reference.zh-CN.md`](docs/guides/upstream-sync-reference.zh-CN.md)
>
> **✅ 2026-08-21（续）最新交接：1oneUI 136 提交上游同步已完成并推送**：[`docs/guides/session-2026-08-21b-1oneui-upstream-sync-handoff.zh-CN.md`](docs/guides/session-2026-08-21b-1oneui-upstream-sync-handoff.zh-CN.md)——`one-main` 已推进到 `e613573e1`，169 处显式冲突全部解决 + 额外核出 13 处 3-way merge 静默删除的 fork 独占内容 + 约 50 处自造成的 tsc 错误全部修复（0 tsc / 0 lint / format:check 通过 / 4176 测试通过）+ 真机 CDP 验证中发现并修复两个真实回归（渲染层白屏、Slack/Discord 品牌泄漏）。下方 2026-08-21 那条记录的「1oneUI 135 提交同步完全未开始」结论已随本次同步完成。⚠️ 「团队模式/媒体管线前端半成品」等四项**不是全部完成**——真机点开逐项复核后：团队模式（#3893）与媒体相对路径（#4103/#4105）✅ 真机/功能验证通过；单色 logo（#3614）组件正确接入但当前数据集找不到真正单色素材、视觉效果未 100% 目视确认；**反馈邮箱字段（#4096）实际未实现**——最初只按 grep 到符号就判定完成是错的，界面上根本没有邮箱输入框，只是静默读取已登录账号的 email，仍是待办，详见新文档 §2.1。⚠️ 主仓库 worktree `D:\aionui-m0\1oneUI` 本地还停在旧提交且有一批与本次同步无关的脏文件未清理，接手前先看新文档 §0/§2.2。⚠️ `AIONUI_BACKEND_LOCAL_PATH` 环境变量已被上游改名为 `AIONUI_BACKEND_BIN`。
>
> **🚧 2026-08-21 最初交接（背景，结论已被上面那条覆盖）：**[`docs/guides/session-2026-08-21-1onecore-upstream-sync-handoff.zh-CN.md`](docs/guides/session-2026-08-21-1onecore-upstream-sync-handoff.zh-CN.md)——1oneCore 149 提交上游合并已完成并推送到 `one-main`（含两轮回归测试收尾修复 + 真机 CDP 核验）。文档里核实过一批 GitHub PR 的落地状态：`#4097`/`#4078`/`#4090`/`#4102`/`#4079`/`#4091`/`#4100`/`1oneCore #884`/`#886` **已确认完成不用重做**。1oneCore 后端完整回归套件重跑（`cargo test --workspace`，上次停在 34 个二进制零失败）仍是遗留项，与 1oneUI 无关。
>
> **✅ 2026-08-18 那件停在半路的事已全部收尾**（视觉委托治理，总清单话术 A）：三仓均已提交推送（aionrs `051ff54` / 1oneCore `38cdf307` / 1oneUI `fe56b8e3c`），临时 `[patch]` 块已删、内嵌已重编、真机两条判据均已通过。**不需要再接手。**过程与真机记录见 [`docs/guides/handoff-2026-08-18b-vision-delegate-wip.zh-CN.md`](docs/guides/handoff-2026-08-18b-vision-delegate-wip.zh-CN.md) §0.5。⚠️ 那轮踩到的教训值得记住：**工作区里只要挂着临时 `[patch]` 块，"本机编译通过"就不能当作可推送的判据**——它会把跨仓依赖的版本错配整个隐藏掉（当时确实因此推出了一个主干编不过的提交）。
>
> **📋 想知道「现在还剩什么没做」只看这一份：**[`docs/guides/handoff-2026-08-18-remaining.zh-CN.md`](docs/guides/handoff-2026-08-18-remaining.zh-CN.md)——2026-08-18 收尾后的剩余事项。**头号未决项是 P0「视觉委托绕过企业治理」**（新加的读图能力会调用一个额外的模型，它既不查 allowlist、token 用量也被丢弃，成本上限与用量看板都看不见——与媒体生成上踩过的是同一类洞）。另含**会话分叉已确认但未修的语义问题**（UI 按消息分叉、引擎按 turn 截断，模型会依据用户看不见的内容作答）、两条人工项、以及 §6 的四条方法论（其中「守卫存在 ≠ 守卫覆盖」是本轮所有缺陷的共同形状）。⚠️ **当前 2.1.55 安装包不含最后四个修复**（含一个安全修复），要发得重编内嵌 + 重新打包。
>
> 上一份 [`handoff-2026-08-17-remaining.zh-CN.md`](handoff-2026-08-17-remaining.zh-CN.md) 列的 10 项**已全部处置**，其顶部有逐条结论，并**刻意保留了三条被证伪的前提**（会话分叉后端「已合入」、`eb4bd7f7`「撞 fork 改动」、`b678d839e` 与它「配对」）——留着是因为它记录了「哪一类判断最容易写错」：三条全是「某某已存在/不存在」这类**存在性断言**。
>
> **🔄 要做上游同步先读这份：**[`docs/guides/upstream-sync-backlog-2026-08-16.zh-CN.md`](docs/guides/upstream-sync-backlog-2026-08-16.zh-CN.md)——v2.1.54 发布后**仍待处理的 60 条**（34 条本轮刻意推迟 + 26 条 triage 后新增），已按功能组把**前后端配对**好，每组标注冲突面/收益/难度/是否必须，并给出建议顺序。§1 是**评估同步时必须避开的九个坑**（含「前后端必须成对合，已踩三次」「改动面极大但零冲突的提交风险最高不是最低」「3-way merge 会零冲突静默删掉 fork 独占模块」「fork 六个不能被上游覆盖的架构分歧点」「写更新说明要按代码核实不能按 triage 清单」）。
>
> **📦 打 Mac 包之前必读：**[`docs/guides/mac-packaging-preflight.zh-CN.md`](docs/guides/mac-packaging-preflight.zh-CN.md)——Mac 是这个仓库**唯一带质量门禁的路径**（Windows 本地打包完全不跑 lint/format/tsc/test），所以主干上积累的格式漂移与红测试全都攒到打 Mac 包那一刻爆出来，且失败会伪装成「Mac 构建失败」。2026-08-14 连挂三次全是这个原因，没有一次是 Mac 本身的问题。**触发前先把那份文档 §1 的四条跑完。**
>
> 跨仓改动的完整来龙去脉记在下面这些 session 文档里,**不要只看代码**。
>
> **⚠️ 品牌铁律(2026-08-13 用户明确扩大范围：不再只是"每次上游同步后"，而是任何改动收尾前都要做)**：安装器错误弹窗曾漏改品牌名(显示名早是「One Work」,但 NSIS/PowerShell 安装器脚本 `resources/windows/**` 里手写硬编码的错误文案还是「AionUi」,不走 i18n/不走 tsc/不走常规测试所以一直没被发现)。**"批量刷品牌"绝不能只刷 i18n locales**,必须按 [`upstream-sync-reference.zh-CN.md`](docs/guides/upstream-sync-reference.zh-CN.md) §5.5「品牌复检铁律」逐类扫全(i18n + 安装器脚本 + 渲染层硬编码 + 测试断言 + 外链),并守住"改用户可见文案、不碰内部标识符/版权头/exe/appId/userData 名"的边界。**2026-08-13 新增一类此前从未检查过的表面：Windows 任务栏/托盘图标 tooltip（`app.setAppUserModelId`）与窗口标题**——`app.getName()`（决定 userData 路径）、`<title>` 标签（决定窗口标题栏）、`AppUserModelID`（决定任务栏 tooltip/分组）是三个独立的身份来源，改品牌只顾前两个会漏第三个；此仓此前**从未调用过 `setAppUserModelId`**，已在 `common/platform/index.ts` + `process/utils/configureChromium.ts` 补上（prod 用 electron-builder.yml 的真实 `appId: com.huanle.oneone.ai`，dev 加 `.dev` 后缀避免与正式安装任务栏分组冲突）。**2026-08-14 再新增一类，且是用户装完包截图打脸抓到的：系统托盘图标 tooltip（`tray.setToolTip`，`process/utils/tray.ts`）**——它硬编码着 `'AionUi'` 跨越了历次改名。**根因是我上一次复检时把"渲染层硬编码"理解成只扫 `src/renderer/`，而托盘、启动期错误弹窗这些用户可见文案全在主进程 `src/process/`**。已同批修掉 macOS 架构不匹配弹窗里的同类硬编码。

> **治本措施（2026-08-14 用户要求写进铁律）**：品牌名不再允许散写字面量——新增单一来源 `BRAND_DISPLAY_NAME`（`common/platform/index.ts`），所有用户可见处一律 import；新增 `tests/unit/process/trayBrand.test.ts` 锁死三件事：①`BRAND_DISPLAY_NAME` 必须等于 `electron-builder.yml` 的 `productName` ②托盘 tooltip 必须来自常量、出现字面量即失败 ③主进程这些文件里不得有用户可见的上游品牌串。**下次改品牌只需改常量 + 跑测试，而不是靠人肉记得扫哪几类。**

> **复检范围更正**："渲染层硬编码"这一类应读作**全部前端可见文案，含主进程**（`src/process/` 的托盘/弹窗/菜单/通知），不是只有 `src/renderer/`。

> **任何改动（不限于上游同步）收尾前，都应过一遍这五类表面：i18n 显示文案 / 渲染层与主进程硬编码 / 安装器脚本 / 任务栏与窗口标识 / 系统托盘 tooltip**，不必等到"下次上游同步"才做。

- **【2026-08-19】团队作战「导出记录」：整支团队的作战过程导出成离线单文件 HTML（**仅本仓，纯前端，后端零改动，不需要重编内嵌**）**：抬头新增按钮，把团队里每个成员（= 每个独立会话）的历史消息一次拉全，产出一份自包含 HTML，含「分列视图（复刻界面多列）」与「全局时间线」两种视图、按成员/关键词筛选、工具调用详情全量保留（默认折叠）、图片内嵌 data URI。**两种视图共用同一批 DOM 节点**（切换时搬家而非复制，否则体积翻倍）。复用既有 `loadConversationMessagePage` / `sanitizeFileName` / `downloadTextContent`，不另造。⚠️ **真机数据抓出两个只有真实数据才暴露的问题**：①一条 ACP 工具调用的 `update.title` 就是整条 15000 字的 PowerShell here-string，**且该消息没有 rawInput——完整命令只存在于 title 里**，所以「直接截断标题」会真的丢数据，正确做法是抬头收一行 + 原文折进折叠块；②模型写的 markdown 普遍在列表项之间留空行，早期实现把松散有序列表切成多个 `<ol>`，`1./2./3.` 全显示成 `1.`。**导出刻意不受 T4 内容审计/DLP 约束**（工具输出里的文件内容与命令原文原样落成明文 HTML，现有 `PolicyDenial` 只管发送链路）——2026-08-19 用户明确拍板「导出不需要约束」，**这是决定不是遗漏，别再当缺口提**。33 条单测（含对转义/hidden 过滤/游标停滞保护的**负向验证**）+ 相关面 1719 条回归全绿 + 产物内交互与应用内落盘均已真机核对。详见：
  [`docs/guides/session-2026-08-19-team-transcript-export.zh-CN.md`](docs/guides/session-2026-08-19-team-transcript-export.zh-CN.md)
- **【2026-08-18】视觉委托绕过企业治理已修（P0，纯后端：1oneCore + aionrs，本仓只有文档；⚠️ 1oneCore 改了 Rust，**必须 `backend-rebuild.ps1` 重编内嵌**）**：`ReadImage` 给纯文本模型读图时委托**一个单独的视觉模型**——这条新的模型调用路径既不过企业 allowlist（管理员踢出清单的模型照旧能被当委托调用），花费也不进账本（企业成本上限完全看不见这笔计费）。与媒体生成踩过的是**同一类洞、同一条根因**：给既有能力开一条新的到达路径，旧路径上「顺手就有」的治理没跟过来，而且缺了不报错。两个洞都已修（trait 注入 `ModelAllowlistGate` + 跨三仓把用量穿回 `record_turn`），顺带修掉「被 allowlist 拦掉时报错说假话」。三条负向验证均做过。详见：
  [`docs/guides/session-2026-08-18-vision-delegate-governance.zh-CN.md`](docs/guides/session-2026-08-18-vision-delegate-governance.zh-CN.md)
- **【2026-08-08·收尾】后台 UI 的 CDP 点检抓到"类写了但没生效" + allowlist 误伤媒体改为可操作（本仓 `63b3720f1`/`e5fa22bcd` / 1oneCore `89029f66`+`af3f7a36`，均已推 `origin/one-main`，**已重编内嵌**）**：**①「企业下发」渠道的编辑删除入口其实一直可点。** `hidden` 和 `flex` 写在同一个 className 里**根本不起作用**——两者都是单类选择器，UnoCSS 输出 `.flex{display:flex}` 在 `.hidden{display:none}` 之后，按源序 flex 赢。真机 CDP 实测：class 确实挂在元素上、`.hidden` 规则确实在样式表里，而 computed `display` 仍是 `flex`，三个按钮可见可点。改成**干脆不渲染**（条件渲染没有 CSS 可以回归）。⚠️ **排查中差点被自己骗过去**：先数 `querySelectorAll('button').length` 得到"托管 4 / 个人 5"，看着像"少了一个所以生效了"——**`display:none` 的元素照样匹配选择器**，判据必须是 `offsetParent` + `getBoundingClientRect` + computed display。复核后：托管行 0 个操作按钮（DOM 里也没有），个人行 4 个。全仓扫过一遍，`hidden` 与 flex/block 同现的其余位置都是 `overflow-hidden`（`\bhidden\b` 会匹配进去）或 `hidden md:flex` 响应式写法（靠 media query 生效，是对的），**只此一处是真错**。**②allowlist 拦掉媒体时给不出出口。** allowlist 是聊天与媒体共用的一份清单（`check_media_allowed` 刻意如此，避免两套互不知情的规则），代价是管理员填完聊天模型就把全公司图片/视频生成一起关掉，而用户只看到"blocked by company policy"。现在被 allowlist 拒绝时点名杠杆：哪个模型、去哪加、以及"这一份清单同时管聊天和图片视频"；输入框也补了同样的说明并把占位符加上媒体模型。**真机复核**：设一个只含聊天模型的 allowlist → 媒体 precheck 返回新文案、清单内模型仍放行 → 还原原状。**③未知模型计价为 0 打 warn。** 0 成本不只是报表难看——它不消耗成本上限，上限对那个模型悄悄失效；内置费率表按模型名匹配，自带命名的网关（很常见）一条都匹配不上。⚠️ **刻意不发明兜底费率**（那会改变管理员的上限实际拦什么，属定价决策），只让它可诊断；新增测试把"未知模型仍为 0"钉死，正是为了让将来任何人改成非 0 时必须明知自己在做定价决策。**④顺带**：`record_media_usage` 上一轮被我加到 8 个参数顶破了 clippy 的 `too_many_arguments`（我顶破的按 ratchet 该我修回来），收成 `MediaUsage` 结构体——七个大多是原语的值、好几个 `i64`，调用点顺序写错照样能编译然后静静把错的东西计进账；clippy 已回到本轮之前的水平。
- **【2026-08-08·真机】重编内嵌后真机验证：一次通过 + 两个只有真机才抓得到的缺陷（本仓 `16946aa3e` / 1oneCore `b03d9f82`+`ba545d5f`，均已推 `origin/one-main`，**已重编内嵌**）**：单测全绿、代码看着对，真机跑一遍才暴露两条。**①agent 发起的生成，账本里仍然 `conv=None`**——原因就写在 `startMediaJob` 自己的注释里："渲染层知道自己的会话，MCP 调用方不知道"。媒体 MCP 是 stdio 子进程，除 env 无从得知服务哪个会话，于是**最常见的那条路（agent 调工具）恰好是留不下踪迹的那条**；上一轮加的 `conversationId` 只帮到发送框那条路。修法=照 `AIONUI_MEDIA_WORKSPACE_DIR` 的既有做法加 `AIONUI_MEDIA_CONVERSATION_ID`，两个工厂注入、薄壳读出随 generate 带上。**②走 repo 行进会话时，媒体 env 一个都没注入**——媒体 env 的注入原本只写在**快照**路径，而本轮 `session_mcp` 修复让媒体 MCP 是以 **repo 行**身份进会话的，`row_to_mcp_server_config` / `row_to_sdk_mcp_server` 从来不注入它。后果是工具能用但两件事都丢：产物落兜底目录、用量归因为空。⚠️ **我一开始把"图片落在 `%APPDATA%\1one-Dev\1one\` 而不是会话工作目录"归因于"agent 没传 workspace_dir"，是同一个根因，第一次判断错了**。**教训：给一条既有能力开一条新的到达路径时，要顺着新路径把沿途所有 per-conversation 上下文数一遍——旧路径上"顺手就有"的东西，新路径上一件都不会自动跟过来，而且缺了不报错。**

  **真机实证（都是同一个二进制上的前后对照，不是"看起来对"）**：迁移 010/041 在真实 dev 库干净应用（后端起得来、`GET /model-channels` 返回 `{"success":true,"data":[]}`、`GET /api/providers` 不 500）；**模型渠道端到端**——建渠道→列表逐字节搜不到真实 key（命中 0）→签发 token→三种协议形状（`/v1/chat/completions`、`/v1/images/generations`、`/api/v1/services/aigc/...`）路径**原样抵达假厂商**且 Authorization 是**公司凭据**而非成员令牌→无/伪造/Basic 三种 auth 全 401 且**根本没到厂商**→停用渠道后立刻 401→编辑不带 key 时 `hasKey` 仍为 true→成员端物化出 `managed_by=enterprise` 的 provider 且个人 provider 未被动→托管行 update/delete 均 400 而个人行 200→authoritative 空同步清掉托管行、保留个人行→**环境清回 0 渠道 / 2 个原有 provider / 无托管残留**。**媒体链路**——在一个按 cron/数字员工形态（无前端快照）建的会话里，注入结果是 `["aionui-image-generation","ftshare","stock-sdk","team-search"]`，**媒体在、而同为 builtin+enabled 的 `one-export-pdf` 与 `one-team-knowledge` 不在**（这条正好实证了"刻意只放这一个"真的成立）；agent 真的调用工具并出图，`POST /api/one/billing/media-precheck 200` 被命中；修复后的那次 `conv=2693bfb1`（前两次同一二进制上都是 `conv=None`）且产物落进会话工作目录而非兜底目录。测试会话与测试图片已全部清理。

- **【2026-08-08·续】企业模型渠道 Stage B：成员端真的能用了（本仓 `cd3b1c6c6` / 1oneCore `598dd32a`，均已推 `origin/one-main`，**未打包**；⚠️ 1oneCore 改了 Rust 需 `backend-rebuild.ps1` 重编内嵌）**：Stage A 把渠道建在了服务器上，但成员端看不见也用不了，这一半补上。**核心形态**：每个可见渠道在成员机器上物化成一条**普通的 `providers` 行**，于是聊天、两条 ACP 桥接、三种媒体适配器形态**零改动**认得它；与自配渠道只差两点——`base_url` 指向公司模型代理而非厂商，`api_key` 存的是成员那枚**可撤销渠道令牌**而非公司凭据（真凭据留在服务器，由代理转发时替换）。所以 `ManagedChannelPayload` **结构上就没有放厂商密钥的地方**。**新增**：迁移 041 `providers.managed_by`（NULL=自配、`'enterprise'`=下发，个人版恒 NULL）、`aionui-system/src/managed_provider.rs`（按渠道 id 派生 provider id 所以同步是 upsert 不堆行；authoritative 对账；**个人 provider 任何情况都不覆盖**，同 id 只记 conflict 跳过——覆盖会悄悄换掉用户自己录的密钥）、`POST /api/providers/sync-model-channels`（走 `httpPostLocal`，渠道从公司服务器拉、但必须写进**本机**provider 表）、管理员后台「模型渠道」页（**密钥永不回显**，因为服务端任何接口都不返回它；编辑留空即"保留原有密钥"，这才让改名/换作用域不必再翻一遍凭据）、模型设置页「企业下发」标签 + 隐藏编辑删除入口。**托管行的 update/delete 后端直接拒绝**——同步下一轮本来就会还原，放行只会产生"改完过一会儿自己回滚"，是三种结果里最让人困惑的那个。⚠️ **两处 authoritative 判断错了都会静默删掉用户还在用的渠道**：拉不到列表直接返回（离线优先，同技能与 MCP）；**列表拿到了但有渠道取不到令牌时，这一份就不是完整集合，只写不删**（后者做过负向验证：写死 `true` 后正是那条测试失败）。⚠️ **过程中修掉一个自己写出的、只在运行时炸的缺陷**：给 provider 的 INSERT 加了第 18 个 bind 却**漏改 SQL 的列清单与占位符**（仍 17 个）——`sqlx::query` 这条路不做编译期校验，**`cargo build` 一路绿**，是新加的同步测试真的走了 `create` 才撞出来。**教训**：给 `sqlx::query` 加 bind 时，列清单/占位符/bind 三处必须一起数，编译器不会替你数。前端新增 8 条测试（企业+媒体 363 绿）、后端新增 9 条（aionui-system 172 全绿）；tsc / lint 0 error / check-i18n 通过，13 语言 key 补齐（其中 10 种暂用英文占位）。**剩真机端到端**：配一条真实渠道 → 成员机器同步出 provider → 发一条真消息经代理 → 生成一张图 → 撤销后立即失败。进度以活文档为准：
  [`docs/guides/enterprise-china-market-gtm.zh-CN.md`](docs/guides/enterprise-china-market-gtm.zh-CN.md)
- **【2026-08-08】媒体与企业版打通的三个真缺口 + 企业统一下发模型渠道（服务端）**：本仓与 1oneCore 均有改动，**未打包**；⚠️ 1oneCore 侧改了 Rust，必须 `backend-rebuild.ps1` 重编内嵌才生效。产品视角的完整盘点（为什么签不下单、竞品对标、按签单路径排的 T1~T9）在新建的活文档 [`docs/guides/enterprise-china-market-gtm.zh-CN.md`](docs/guides/enterprise-china-market-gtm.zh-CN.md)，**排期以那份 §七 为准**。

  **①客户端模式下媒体（以及整个计费面）的企业管控是空转的**——`/api/one/billing` 不在 `GOVERNANCE_PATH_PREFIXES` 里，precheck 与用量上报打的是**员工本机 aioncore**；而成员资格只存在于服务器，本地 `one_enterprise_members` 是空表 → `resolve_enterprise_id` 恒为 `None` → `check_send_allowed` 第一行就放行，用量写进没人看的本地库。**而客户端模式正是企业唯一真实的部署形态**，即卖出去的成本上限在真实部署下是装饰件。⚠️ 同一条路径让**聊天的 SendGate 在客户端模式下同样空转**（既有问题，非媒体引入）。修法两半：把团队知识库那套"渲染层把企业端点推给主进程"的机制从 `teamKnowledgeMcpServer.ts` 抽成通用的 `process/services/governanceEndpoint.ts`（**主进程读不到 renderer localStorage**，这是它存在的全部理由），媒体管控改走 `governanceFetch`；同时把 `/api/one/billing` 加进 governance 前缀，企业后台的订阅与用量页也不再读本机空表。

  **②数字员工 / 定时任务 / 团队 / 存量会话全都用不了媒体**——后端两个 agent 工厂都有 `if !selected || row.builtin { continue; }`，内置 MCP 结构上进不了会话，唯一通道是前端 `withBuiltinMediaMcp` 写进会话快照，而**它只挂在 Guid 新建会话页一处**。于是企业最值钱的自动化场景一个都不会生图生视频。新建 `factory/session_mcp.rs` 把两个工厂的选择规则收口到一处，媒体这一个内置 MCP 跟着自己的 `enabled` 走（与前端同一条规则）；**刻意只放这一个**，否则每个 agent 都会拿到 PDF 导出与团队知识库。⚠️ 顺带发现 **`aionui-ai-agent` 的 lib 测试目标在主干上根本编译不过**（`merge_session_snapshot_mcp_servers` 加了 `workspace` 参数没改调用点），也就是那 793 条测试一直没跑过，已修。**做了负向验证**：把 auto-inject 判定改成恒 false，正是该失败的两条失败。

  **③审计看不见媒体**——`list_agent_audit` 的 detail 只抽 command/path/url，**不抽 prompt**，媒体调用只显示工具名与空白；发送框直发那条路根本不产生 message。已补 `$.args.prompt` / `$.input.prompt`，并给用量上报补 `conversationId`（`one_usage_events.conversation_id` 此前对媒体恒为 NULL），让发送框那条路至少在企业账本里可归因。**刻意不记录 prompt 内容**——那是内容留存决策，属管理员而非产品默认。

  **④企业统一下发模型渠道 Stage A（服务端）**：`one-devops` 三个 registry 从来没有 provider registry，于是每个员工必须自己粘贴模型 key（图片/视频尤其贵），采购最不能接受。用户拍板**服务器代理**（key 只在服务器解密、永不出服务器）+ **企业级作用域可按项目组覆盖**。新增迁移 `010_provider_registry`（`one_provider_registry` + `one_provider_channel_tokens`）、`provider_channel.rs`（CRUD/ACL/加密/token 签发撤销解析）、`model_proxy.rs`（**路径透传**反向代理 `ANY /api/one/model-proxy/{cid}/{*path}`，请求与响应双向流式）。⚠️ **刻意不照抄 `aionui-codex-bridge` 的协议感知代理**：媒体一家就说三种互不兼容的形状（Form A / chat / DashScope-Ark 异步任务），协议感知要为每种写一遍；路径透传让聊天、Form A、Form C 全部零改动工作。⚠️ **成员端凭据是专用可撤销 channel token 不是会话 JWT**——JWT 因成员变动而轮换，用它会让每次人员变动打断所有已配渠道；只存哈希。⚠️ **移除成员时连带撤销 token**：走新 trait `one_org::CredentialRevoker`（同层不能直接依赖，照 `EnterpriseSync` 的既有安排），接在 `invalidate_user_tokens` 里，所以每条杀会话的路径都会杀 token——留给 UI 流程去记得调用的步骤就是会被忘掉的步骤。**15 条测试 = 11 单测 + 4 条打真实上游服务器的端到端**，最要命那条是把渠道列表序列化后**逐字节搜真实 key**；端到端验的是设计赌注本身（三种协议形状路径原样抵达、成员 token 被换成公司凭据且自己不外泄、撤销后立即在代理层被拦），凭据替换那条做过负向验证。**Stage B（客户端后台 + 成员端物化 + 真机端到端）未做。**

- **【2026-08-07·核对轮】交接文档核对 + 修掉「切走再切回被拽回生成模式」**：本仓 `734df9dd3`（文档核对）/ `673c9e3d2`（修缺陷），已推 `origin/one-main`，**未打包**（纯前端，无需重编内嵌）。**①核对交接文档查出六处与事实不符**，其中最要命的是**三处 Windows 路径里藏着不可见控制字符**——dev 的 userData 被写成 `%APPDATA%one-Dev`、探针目录写成 `D:ionui-m0`，看着像漏打一个字符，实际文件里躺着**字节 `0x01`/`0x07`**（路径经某层正则/shell 替换时 `\1` 被当成捕获组引用、`\a` 被当成响铃符吃掉），`grep` 打印毫无异样、只有 `cat -A` 或按字节 count 才看得见，**照这路径去找目录永远找不到**；其余为 §2 漏记 `ebeb68186` 且把 `3e9617212` 的归属写反、§6 子标题仍是 `5.x`、§7 两行指向同一目录。**②修掉一个用户可见的真缺陷**：`mediaModeStore` 的 `requests` Map **只写不删**，而送框记录"已应用 seq"用的是 `useRef`——**ref 跟着组件实例一起死，store 里的数据不死**，于是送框一卸载重挂（切走再切回、HMR），ref 归零而旧 request 还在，被第二次应用：用户选了视频模型→手动退出→切走切回→**送框自己又跳回视频模式**。修法=request 是一次性指令，应用完即 `consumeMediaModeRequest`（按 seq 匹配后再删，避免误吞期间新到的请求）。⚠️ **该缺陷卡在测试的缝里**——既有 11 条测试覆盖了 seq 递增/会话隔离/卸载清理，**唯独没问"应用之后请求还在不在"**；补的 4 条含 remount 回归，并**做了负向验证**（把 `requests.delete` 注释掉重跑，确认正是那两条失败）。**③顺带**：`outputs/`（含媒体产物与 3.5MB pptx）和 `.workbuddy/` 一直不在 `.gitignore` 里，一次 `git add -A` 就会把多 MB 二进制提交进仓库，已加根锚定规则，**现在 `git status` 应当完全干净**。全量 **3186 绿 0 失败**（较上轮 +4）。**⑦打通专家 agent 与图片/视频模型**（`a8daa3080` / 1oneCore `f73c35e7`，用户拍板方向）：**声明的媒体模型此前对 agent 完全不可见**——`tools.imageGenerationModel`/`tools.videoGenerationModel` 这两个设置键**早于 `model_kind`**，当年在那里选一个是拥有媒体模型的唯一方式，所以下游只读这个键；而产品现在要求用户在模型列表里**声明**类型，这条路留下的键是空的，于是 agent 调工具被告知"没有配置模型"（尽管明明配了），且内置媒体 MCP 的 `enabled` 挂在旧开关上默认关、**工具压根不下发**（发送框从没踩到，因为它显式传自己的模型）。新增共用纯函数 `common/media/declaredModel.ts` 两处回落，**刻意一份实现**（决定"要不要给工具"与"真正跑它"必须同一个答案）；⚠️ **存量安装另有一层**：`enabled` 只在建行时算、更新路径只碰 transport，不补的话新装点亮、已有安装毫无变化，已补**单向启用**（只开不关，删模型不该悄悄抽走 agent 正在用的能力）。**⚠️ 我提的修正（用户采纳）**：原计划"有媒体模型就优先调"，但两条路**不是高低配**——"一只在天空飞翔的猫"AI 模型完胜，"把季度营收做成 30 秒动画"则是代码渲染完胜（生成模型没法渲染真实数字，只会编一个像图表的画面，**比只有一条路更危险**）；工具描述由 `REQUIRED... you cannot produce video yourself` 改为讲清各自擅长什么 + 可组合，判断交给 agent。**⚠️ 两条交付条件**：工具描述由 `scripts/build-mcp-servers.js` 产出，**只在打包时跑、`bun run dev` 不跑**；人设经 `include_dir!` 编译期内嵌需重编 aioncore，且市场目录 upsert 会更新但**用户已安装的那份是 `assistant_definitions` 独立拷贝**需重装该专家。**⚠️ 真机验证时发现打通还差第三层**（`c6dfe0e30`）：`enabled=true` **不等于 agent 能用**——两条 agent 路径（`factory/acp.rs:497`/`factory/aionrs.rs:473`）都有 `if !selected || row.builtin { continue; }`，**builtin MCP 被显式跳过**，只能靠前端塞进会话快照且要求被显式勾选；实测视频专家 `mcp_server_ids: null`、**库里 12 个会话该字段全是 null**，即内置媒体工具**从来没进过任何会话**。已补 `withBuiltinMediaMcp`（媒体 MCP 跟着自己的 enabled 进快照，**刻意只放这一个**，否则会把 PDF 导出与团队知识库也塞给每个 agent）。**✅ 真机实证**：重启 dev 发一条消息 → 后端日志 `Injecting MCP servers into aionrs session mcp_count=1 mcp_names=["aionui-image-generation"]`。⚠️ **但仍未验到 agent 真的调用并出图**（需花钱走一次真实生成，列为接手第一件事）。**方法论**：这一层不是读代码想出来的，是**先 curl 看真实数据**（MCP 的 enabled vs 每个会话的 mcp_server_ids 全 null）**对不上才回头找到那行 continue** —— 单测里没有"会话快照"这个概念，再多绿测试也照不亮它。全量 **3205 绿**（+15）。**④同轮修掉用户真机截图报的三处发送框体验问题**（`995bf0c41`）：**生成模式下同屏两个模型且相互重叠**——欢迎页左边参数 pill 是真正会跑的 `seedance-2-0-fast`、右边模型选择器是本次发送根本不参与的聊天模型，根因是**会话内两个选择器都做了媒体感知、唯独欢迎页的 `GuidModelSelector` 一行都没有**（⚠️ 本文 08-07 条目那句「~~ACP 选择器与欢迎页未做~~ → 已完成」不准确，欢迎页完成的是**生成入口**不是**选择器媒体分组**）；重叠则因 CSS 那条 `min-width:0` 只覆盖右侧 `.actionConfigGroup`，左侧 `.actionTools` 的 Arco 按钮是默认 `min-width:auto` 的 flex item 拒绝收缩，参数 pill 保持原宽**溢出**画在选择器上（约束已加进 `MediaModeControl.module.css`，三处宿主共享）。**⑤模式图标三缺一**：`video ? VideoTwo : Picture` 让 `off` 和 `image` 落进同一分支，「对话模式」顶着图片图标；改为一模一图（`off`=`Message`），⚠️ `renderModeIcon` 须声明在 `modeMenu` 之前否则 TDZ。**⑥「对话」→「对话模式」**（用户建议，13 语言）：触发器与下拉首项共用 `mediaModeOff` 一个 key，收起时它是唯一的自我说明。四条新测试均做过**负向验证**，真机 CDP 复核图标/文案/四个 pill 横向区间两两不相交。全量 **3190 绿**。
- **【2026-08-07·续】多媒体生成：交接 §5 四项待办全部完成 + 修掉「带附件发消息一律 400」+ 查清项目 Explorer 后端整层缺失**：本仓 `680302a0d`/`f9fe9daf3`/`b8fccd164`/`ebeb68186`、1oneCore `ab76ae9d`，均已推 `origin/one-main`，**未打包**。⚠️ `ab76ae9d` **必须 `backend-rebuild.ps1` 重编内嵌**才生效。**①发版级缺陷：带文件发消息一律 400**——`SendMessageRequest.files` 只收 `Vec<String>`，而桌面端自上游 `26a2e72e8`(project-scoped Explorer #3763) 起对每个附件都发标签对象 `{kind:'upload'|'local'|'project'}`；实测对象四种变体全 400、裸字符串 202，dev 库 20 个会话里带 `[Attached files]` 的只有探针那条，即**附件功能在当前代码里从来没可用过**。已改为 `#[serde(untagged)]` 同时接受两种形状，`project` ref 走**本来就存在的** `ProjectService::resolve_reference`（我一度误报"它不存在"，是组合 grep 把 `pe_id` 匹配到 `scope_id` 又被 `head` 截断）。**②前置事实钉死：agent 的上下文不是从消息表重建**——aionrs 读磁盘会话 `<userData>/1one/aionrs-sessions/sessions/<id>/state.json`、ACP 读 CLI transcript，`insert_raw_message` 唯一生产调用点是团队适配器。**所以"写一条消息给 agent 看"的方案无效**；改走附件通道，并做成**可见**的（结果卡片「引用到输入框」→ 发送框可删 chip → `[Attached files]` 抵达 agent），不自动附加以免给下一条消息悄悄加钱。**③⚠️ 项目 Explorer 整个后端不存在**：`ConversationResponse` **没有 `project_id` 字段**（前端注释却明写来自它）、`/api/projects/*` **0 处路由**、`fs/subscribe`/`fs/delta` **0 处**；补上字段后真机确认面板第一次渲染，但 `GET /api/projects/{id}` 404、树永远空，而 `ChatSlider` 一见 `project_id` 就把"有解释的空面板"换成"没有解释的空树"，**今天净负面故已回退——字段必须与后端一起落地**。这是**上游同款问题的第三例**（`model_kind` → `ChatFileRef` → Explorer）：**上游前端合了、后端那一半没合**，再遇到"前端有、后端像没实现"先按这个模式查。**④生成物落点缺陷**：`input.workspaceDir || process.cwd()`，dev 下写进仓库根、打包后可能是 Program Files，已兜底到应用 `workDir`（**必须惰性 require**，直接 import `initStorage` 会打断只 mock 了部分模块的 MCP 集成测试）。**⑤参考图本来就通、纯粹没入口**：三个适配器一直消费 `inputUris`，已补 placeholder 提示 / `+` 改「添加参考图」/ 原生对话框传 `filters`（`accept` 只管 WebUI 管不住 Electron）/ 非图片文件按扩展名过滤并说明去处；**刻意不支持"读文本附件当提示词"**（会让实际发出的提示词与输入框显示的不一致）。**⑥真机通过**：文生图、**图生视频**（`refs:1→done`）、附件抵达 agent、欢迎页发起生成；**图生图仍不通**，报错来自网关（multipart → `/v1/images/edits` 被回以 Go 的 JSON 解析错），需要用户测试台「GPT Image」的真实契约，**别猜**。⚠️ **附件入口有两套实现**（会话 `FileAttachButton` / 欢迎页 `GuidActionRow`），改文案或过滤要两处都改；而 `MediaModeControl` 是三处共用。**接续必读**：[`docs/specs/media-generation/handoff-2026-08-07.zh-CN.md`](docs/specs/media-generation/handoff-2026-08-07.zh-CN.md)（已就地更新，§1 现状 / §5 四项结论 / §5·补 剩余项 / §9 本轮六条新发现 / §10 十九条不要重犯）。
- **【2026-08-07】多媒体生成：视频端到端跑通 + 花费可见 + 一批显示层缺陷**：本仓 11 个提交 `3e9617212`→`49e986062`、1oneCore `89dbb5ca`，均已推 `origin/one-main`，**未打包**。**①视频第一次真机出片**（此前从未成功）——用户给出可运行脚本后定位到真实入口是**同一台网关主机的 `POST /api/seedance/createVideo`**（`/api/` 前缀，不在 `/v1` 下），新增 `seedance-gateway` 驱动；⚠️ **08-06 交接文档 §3「网关没代理 Ark 任务 API、解法是指向方舟直连」整节作废**（方向对但证据是用无效 token 得出的，且解法错）。⚠️ **不硬编码内网主机**：走既有的「用户声明 `media_endpoint` 盖过内置目录」通道。⚠️ **该网关与 ark 有三处结构性差异**（轮询是带 body 的 POST、取消 model 在 query、参数是 JSON 字段而非拼进提示词——**拼错会让 `--flag` 变成画面内容**）。**②1oneCore `ModelSettings` 补齐三个字段**：`model_kind`/`media_endpoint`/`media_unit_price_usd` 此前经 `PUT /api/providers/:id` 被 serde **静默丢弃**，导致 08-06 那套「用户声明模型类型」架构**在真机上从来没生效过**；改完必须重编内嵌。**③花费全程可见**（发送前预估 + 完成后实付，镜像后端费率表并用 Rust 侧测试向量当锚点防漂移；用户单价优先，无费率则说「费用未知」而**绝不显示 $0.00**）。**④修掉的既有缺陷**：`settings-switch-on` 的绿色开关规则因 `:global()` 写在普通 css 里**被构建丢掉、从来没生效过**（WebUI/企业远程两处一起修好）、欢迎页「选中的模型」与「可选列表」用了两个不同列表导致选中不能聊天的模型、空 body 报错在诬告 API key。**⑤按用户要求做的显示改动**：选择器每行显示类型标签（推翻我自己"是噪音"的判断）、标签去掉问号改为点击直接编辑、空「项目」面板说明原因并点名工作目录。**⑥入口重做（用户拍板）**：媒体模型**并回模型选择器**分三组（provider 聊天模型 / 图片生成 / 视频生成），选中媒体模型不设聊天模型而是**把发送框切进对应生成模式**，选回聊天模型则退出——此前媒体模型只在设置里配，用户标了「视频模型」却在会话里找不到它，且入口是个写着「对话」的按钮毫无提示。⚠️ 判定统一走 `isMediaGenSupported()`（与设置页、执行路径同一谓词，别在选择器里另写一套）；跨组件走 `mediaModeStore`（选择器在头部、模式 state 在发送框，是不同外壳的兄弟节点），**请求必须带 seq 只应用一次**否则重渲染会跟用户抢发送框。~~仅 aionrs 选择器已改，ACP 选择器与欢迎页未做~~ → **已于同日续做完成，见上一条**。**⑦实证**：选了工作目录，生成物就落进该目录（`D:\aionui-m0\media-project-probe\img-*.png` 与 README 并排）。**接续必读**：[`docs/specs/media-generation/handoff-2026-08-07.zh-CN.md`](docs/specs/media-generation/handoff-2026-08-07.zh-CN.md)（含探测方法论「必须带负对照」、我自己两次判断失误的复盘、环境里留下的测试残留清单、以及剩余第 1 项「agent 看不见发送框生成结果」动手前**必须先钉死的那个事实**）。
- **【2026-08-06】多媒体生成五阶段全部完成 + 白名单架构被用户推翻重做 + 视频跑不通的真因更正**：本仓 `e13287455`→`51e73861e` 九个提交、1oneCore `e904eaa7`+`1bf8accc`，均已推 `origin/one-main`，**后端已重编内嵌**。**①用户推翻「按模型名匹配」这套白名单**（原话「不能每次都让开发者去放行」）——已改为 `ModelSettings.model_kind` 用户声明优先，模型列表加彩色类型标签；⚠️ **但 Form C 的 `IMPLEMENTED_ENDPOINT_STYLES` 不是白名单不能删**（它管"有没有写过这个协议的代码"），且**视频刻意不猜 endpoint**（各家异步接口互不兼容，猜错只在调用时才失败）。**②视频跑不通的真因已更正，旧结论作废**：不是「key 无权限」（用户指出就一把 key），实测**该网关对 `/v1/*` 下未配置路径一律返回 200 空 body**（连不存在的路径也是），即**它根本没代理 Ark 任务 API**；解法=视频 provider 指向方舟直连，代码无需改。**③修掉的真缺陷**：ark 驱动缺版本段（经网关视频 100% 必挂）、2xx 空 body 抛裸 SyntaxError、`tools.videoGenerationModel` 只读没写（视频在设置层面是死的）、用量上报只挂 TCP 路径（渲染层新路会静默逃过企业统计）、**生成图片缩略图从来没显示过**（走 `/api/fs/image-base64` 对该路径直接 400，已改 `one-media://`）。**④新增能力**：`one-media://` 流式协议（`file://` 被 webSecurity 挡死而视频不能走 base64）、sora/cogvideox/kling 三驱动、企业成本上限与 allowlist 纳入媒体、发送框媒体模式（模式切换+目录驱动参数 chip）。**⑤用户提出但未做**：模型选择器里没有类型标签（注意有 4 个独立选择器）。**接续必读**：[`docs/specs/media-generation/handoff-2026-08-06.zh-CN.md`](docs/specs/media-generation/handoff-2026-08-06.zh-CN.md)（含 CDP 验证坑：渲染层走 WS 非裸 IPC、Arco 下拉要发 PointerEvent 序列、`/@fs/` 才能 import 应用模块）。
- **【2026-08-05】多媒体生成（图片/视频）短板补齐——架构已定稿，**阶段一（图片 Form A + 能力目录）与阶段二（异步任务引擎 + Form C）代码完成、已推送、未打包\***\*：改造前=图片只有 chat 多模态回图（"Form B"）一条窄路（DALL·E 3/gpt-image-1/Flux/SD/万相/即梦全部结构性接不了）、视频为零、根因=缺「异步媒体任务」抽象层。**阶段一已落地**：新建 `common/media/`（类型层一次做全，视频与 Form C 形状现在就进接口）+ 声明式能力目录 `catalog/`（替代模型名正则猜能力）+ `OpenAiImagesAdapter`（解锁 DALL·E/gpt-image-1/Flux/SD/Seedream/CogView）+ Form B 迁入 `ChatMultimodalAdapter` 并**修掉"多图只取第一张"**（`common/chat/imageGenCore.ts` 已删除）+ 工具 schema 扩参（不支持的参数裁剪并告知 agent 勿重试）；tsc/lint/i18n/全量测试 2947 绿（2 个失败是 previews 既有 flake，单独跑绿）。⚠️ **三个既有测试因刻意行为变更而更新**（allowlist 断言 gpt-image-1 从 false→true 正是本轮目的；env 新增 `AIONUI_IMG_PROVIDER_NAME` 会让存量用户升级后首次启动多一次 MCP sync，收敛非缺陷）。⚠️ **过程中修掉一个自己写出的真缺陷**：仅按模型名匹配无法区分"兼容网关提供的 SD"与"Stability 原生 API 提供的 SD"（判据是主机不是模型名），已加原生协议主机压制表。**阶段二已落地**：`process/services/mediaJob/`（状态机+原子持久化+**重启只恢复已拿到 task_id 的 job，提交前中断的绝不重新提交以免重复扣费**+每 provider 并发 2+取消）、`TaskPollAdapter`+dashscope/ark 两驱动（**复用用户已配的 chat provider 凭据，换 path 不换渠道**）、`imageGenServer` 薄壳化走 TCP（脚本名与服务名刻意不改以免破坏既有安装识别）、**`AIONUI_IMG_API_KEY` 不再下发子进程**（生成已在主进程，执行时按 providerId 现查）、新增 `one_video_generation`/`one_media_job_status`；全量 2976 绿 + 真实帧协议端到端集成测试（断言凭据不出现在返回帧和 job 文件里）+ 打包冒烟（无端口时报明确错误不静默回落）。⚠️ **又修掉两个自己引入的缺陷**：kling/sora/cogvideox 只有目录条目没有驱动却被判"支持"（已加 `IMPLEMENTED_ENDPOINT_STYLES` + 与驱动注册表一致性测试）、媒体 TCP 端口每次启动重分配但 bootstrap 从旧 env 搬运导致端口过期（已抽 `buildImageServerEnv` 两路共用）。⚠️ **两个驱动的线格式只用假服务器验过骨架，没用真 key 打过真实端点，接续者应优先真机联调**；渲染层媒体 UI（播放器/进度卡片/画廊/视频附件）仍是阶段三待做。架构决策（六条，含「执行搬主进程 MCP 变薄壳走 TCP」「能力目录数据驱动干掉正则」「媒体字节永不过 TCP 只传路径」）见设计文档；五阶段路线图=①图片 Form A ②异步 job 引擎+Form C ③视频端到端 ④参数/配置 UX ⑤企业管控闭环（**媒体生成现在完全绕过成本上限与 allowlist，真实合规缺口，跨仓 1oneCore**）。**接续任何多媒体相关工作前必读两份文档，做完必须更新进展文档\*\*：设计 [`docs/specs/media-generation/architecture.zh-CN.md`](docs/specs/media-generation/architecture.zh-CN.md) / 进展归一 [`docs/specs/media-generation/progress.zh-CN.md`](docs/specs/media-generation/progress.zh-CN.md)。
- **【2026-07-31】README 按 2.1.51 能力面全面刷新（本仓 `4918d8c66` / 1oneCore `5b2b61e8`）**：上一轮 README 更新（07-29 `ed075f7a4`）之后产品面变化很大，README 已落后。**新增章节**：MCP 工具（内置默认连接 + 逐工具 schema 校验 + 与用户真实 Claude/Codex 配置隔离）、定时任务（执行助手可选任意专家 / 新建会话 vs 持续会话 / 队列保护）、企业版的「授权许可、订阅席位与模型成本管控」整节（授权码离线激活、free/team/enterprise 三档与功能门控、成本上限与模型 allowlist、用量看板、备份恢复脱敏且仅 system_admin）。**精确化**：助手体系写成三档「我的助手 / 官方助手 **22** / 专家市场 **252**」（数字以真机 UI 三个 tab 为准，不是拍脑袋）、渠道按实际状态区分已上线与即将上线、企业版三层模型改为按**数据作用域**讲各层管什么、SSO 补标准 OIDC。**收录用户新替换的 10 张截图**（项目组 ×3 / 企业版后台 ×2 / MCP 工具 / 技能 / 定时任务 / 渠道配置 / 服务模式选择）。**⚠️ 顺带修掉一处图文不符，也是这类活的通用坑**：`工具使用.png` 早被换成"中文界面下的仓库架构分析"，但 README 仍拿它当「切换为英文界面后的效果」的配图——**同名图片被替换时 README 不会报错，必须逐张开图核对说明文字**，本轮 8 张同名替换图全部开图核对过。1oneCore 侧同步补齐领域 crate 清单（漏了 `one-enterprise`/`one-billing`/`one-platform` 三个）。两份 README 在 HEAD 上本来就不满足 prettier（`--write` 会重排全部表格产生无关巨 diff，按 ratchet 未动）。
- **【2026-07-31】选中的专家带着失效模型时建不出团队（本仓 `c6b1284d7`）**：真机选一个专家当队友点确认，报 `no enabled provider offers model 'opus'` 直接建不出团队。**根因=助手自带的模型只是偏好不是保证**，`last_model_id` 尤其会比它来源的 provider 活得更久（市场人设自带一个 → provider 被换掉 / 模型下线 → 值还留在原地），而 aionrs 后端在服务端要拿这个名字去**已启用的** provider 里查（`provisioning.rs::resolve_provider_for_model`），查不到就拒绝建团队。本机实测不止 opus：`opus` 4 例、`deepseek-v4-pro` 1 例、`glm-5-2` 1 例，其中三例是 aionrs 后端——**三个专家一旦被选进团队必复现**。修法=交给服务端之前先校验，`resolveAionrsDefaultModel()` 改成 `resolveAionrsModel(preferred?)`：偏好模型只在某个启用 provider 真的提供它时才采用，否则回落到第一个可用模型（占位符与陈旧偏好两种情况就此走同一条路）。
- **【2026-07-31】数字员工接上专家库与模型（修 aionrs 员工 100% 必挂的 `Provider '' not found`）**：用户真机三个问题——「知识库未就绪」文案、运行历史全是 `Provider '' not found`、创建弹窗写内部代号 `Aionrs` 且不能选专家。**链路**：`CreateAgentModal` 无模型字段 → `one-employee/service.rs` 硬写 `model: None` → 会话 `model` 列 NULL → `task_options.rs:42` 解析成哨兵 `provider_id: ""` → `factory/aionrs.rs:81-87` 空串查 provider 报错（`task_options.rs:35-41` 的注释甚至把这个报错写成了预期行为——哨兵设计假定调用方一定给 model，员工这条路径从没接上；ACP 不受影响因为 `FactoryContext` 结构上就没有 model 字段）。**做法=逐条照抄定时任务**（`aionui-cron/executor.rs` 的 `resolve_model`/`build_assistant_request`/`r#type = assistant.is_some() ? None : Some(..)` 三段式 + `CreateTaskDialog` 的 UI 形态），不另造。**硬约束**：顶层 `model` 只允许 aionrs，其他类型传了直接 400（`aionui-conversation/service.rs:795-802`）；有 `assistant` 时 `r#type` 被忽略，所以**手动改后端唯一通道是 07-28 加的 `conversation_overrides.agent_id`**。**⚠️ 中途被用户纠正一次理解偏差（关键）**：第一版「专家」下拉喂的是 `selectableAssistants`（按 `enabled !== false` 过滤），真机上只剩 3 条 CLI 助手；用户指出「专家」指的是**官方助手 22 + 专家市场 252**。实测坐实 21 个官方助手 `enabled=false` 被滤掉、市场目录只在搜索时才合并。修正后：专家 = 官方+市场+自建（275 条默认全量可浏览）、**裸 CLI 移出专家列表**（它本质是后端，已有独立字段）、新增「不指定专家（仅用运行后端）」、未启用官方助手点一下自动启用、未安装市场专家点一下自动安装。**新增**：迁移 `employee_004_persona_model` 四列（`assistant_id`/`agent_id_override`/`model_id`/`model`）、共享 hook `useEmployeeAgentBinding`（Create/Manage 共用，后端默认跟随专家+一次性闩锁解耦）、`EmployeeBindingFields`、`employeeDisplay`（品牌名一律查 catalog，aionrs 那行**就是** `1ONE CLI`，永不硬编码）。顺带修「知识库未就绪」→「知识库为空，去上传」+ 可点击跳上传。**验证**：`cargo test -p one-employee` 17 绿、前端新增 23 绿、全量测试与改动前逐文件一致（7 个既有失败全属并行会话在途改动，对本轮模块零引用）、**真机 29 条断言全绿**——核心判据是同一路径报错从 `Provider '' not found` 变成真实网络调用失败（provider 与模型都解析成功了）+ CDP 核对 UI。⚠️ 真机验证时本机真实 dev 库起不来（`migration 19 ... has been modified`，属并行会话在改的行尾校验和问题，与本轮无关），走项目自带的 `AIONUI_E2E_TEST=1` + `AIONUI_E2E_USER_DATA_DIR` 隔离目录绕开。未打包：
  [`docs/guides/session-2026-07-31-digital-employee-expert-model-binding.zh-CN.md`](docs/guides/session-2026-07-31-digital-employee-expert-model-binding.zh-CN.md)
- **【2026-07-30·三条并行会话之一】团队唤醒 38 秒冷启动定位与修复（aionrs `02f3c50` / 1oneCore `ad407cdb`+`a229a3eb`+`1133f33c`，均已推 `origin/one-main`；本仓仅文档）**：用户从上游同步团队作战功能后反馈"拉起团队要花很长时间"。**慢的不是团队功能**——`aionui-team` 骨架 2 毫秒就起好，38.3 秒全在 leader 成员的 agent 运行时上（队友是懒唤醒的）。**①`aionui-runtime` 的 `cached_managed_runtime()` 缓存了 runtime 对象却没缓存"它已被验证过"**，每次命中仍 spawn `node`/`npm`/`npx` 三个 `--version`（实测 6.6s，每个 agent 冷启动都付一次），已加 5 分钟 TTL + 文件系统层复核，**6.6s → 7ms**；**②aionrs `aion-mcp::connect_all` 要把所有连接排干才返回**（结果直接喂工具注册表和系统提示），所以 `DEFAULT_STARTUP_TIMEOUT_MS` 就是"用户等多久会话才可用"的上界，30s 被一个起不来的 `stock-sdk` 每次吃满，降到 **15s**。真机 CDP 实测 **38.3s → 15.5s**，热路径 1.1s，ACP 团队路径无回归。**⚠️ 真正的底层根因在代码之外**：本机启动一个 node 进程要 **1~1.7 秒**（正常几十毫秒，Defender 逐进程扫 89.8MB 的 `node.exe`），`npx -y <pkg>` 是多层 node 进程叠加所以慢且不稳——**治本是给托管 node 目录加杀软排除，属安全设置需人工执行**。**⚠️ 引入一处功能性回退**：`stock-sdk` 慢时被 15s 砍掉，69 个股票工具不可用。**⚠️ 三条会话的交汇已核对并写进文档 §十一**：aionrs 那 2 个"无主"未提交文件是本轮的（已推）；另一会话报的 `aionui-runtime` 红 **8 条**是对的（我最初带模块过滤只看到 3 条，**报既有失败数必须跑完整 crate**），本轮修好 2 条剩 6 条；**主检出那份 Jul 30 22:26 的 bundled 后端正是本轮编的**（含本轮修复但不含另一会话的 `f70abde2`）——**现在没有任何一份 bundled 同时含两边修复，打包前须从对齐后的主干重编一次**；另注意 `1oneUI-sync` 与 `1oneUI/.sync-worktree` **都是 1oneUI 的 worktree 且挂在同一分支 `sync-v2142`**（前者在仓库外、干净、本轮用它验证；后者在仓库内、87 个未提交、是另一会话查到的雷，勿动），任一边提交都会推进同一个分支指针。**发现的既有缺陷（未修）**：`list_by_ids_any` 不过滤 `deleted_at`（有测试锁死该语义）→ **从 UI 删掉的 MCP 只要会话还记着 id 就照旧注入**；aionrs 的"影子配置" `%APPDATA%\aionrs\config.toml` 会注入每个会话但应用 MCP 管理页看不到也管不了。**📋 该文档 §十二 是 2026-07-30 三条并行会话全部未决项的唯一归一清单（13 项，按「必须人做／等你拍板／可以让 AI 做」分类），含另两条会话移交的打包 bump、License keygen、`enterprise/index.tsx` 合并基线、主检出对齐、`.sync-worktree` 处置等——要知道"这一天还剩什么没做"只看那一节**：
  [`docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md`](docs/guides/session-2026-07-30-team-warmup-cold-start.zh-CN.md)
- **【2026-07-29】三仓上游全量同步（aionrs v0.2.8 / 1oneCore v0.1.53 / 1oneUI v2.1.43，约 148 提交，含团队作战整批）**：本轮两个**明确不采纳**的架构项——①**session-port 迁移**（1oneCore #609 + 1oneUI #3572）：上游把 Claude/Codex 迁到 direct-CLI `SessionAgentTask` 路径，但那条路径 early-return 绕过了 fork 的 **Codex/Claude 自定义模型桥接注入**，只接了第三方 cc-switch 兜底，照搬会让桥接静默退回 cc-switch-only，用户拍板不迁移；上游新代码（`session_agent.rs`/`aionui-session`/`aionui-process`）照常合入但**未接线**，休眠待将来把桥接 repo 串进 `SessionBuildInputs`。②**`acp_tool_runtime` 被 git 静默删除**（**零冲突标记**！自动合并把 fork 独占的 npm 按需下载逻辑整个换成上游的 `managed_cli` 原生二进制方案，靠编译报错才暴露）——已恢复该模块及其在 `cache.rs`/`managed_resources.rs`/`runtime_status.rs`/`RuntimeResourceKind::AcpTool` 的全部支撑代码。**⚠️ 教训：3-way merge 能在零冲突的情况下删掉 fork 独占模块，"冲突解完"绝不等于安全，必须全量编译+测试。** 其他要点：migration 撞号 4 个已重排 037-040（另删一个逐字节重复的）；**#3696 助手列表重做**把 `MyAssistantRow` 换成卡片式，fork 的两处专家市场逻辑已移植（否则市场装来的助手**不可删除**且**显示不出中文名**）；i18n 40 个冲突块脚本化处理（上游侧作结构模板 + fork 值优先 + `JSON.parse` 当闸门，脚本按设计拒绝丢 fork 独有 key 的合并）；品牌复检刷 52 处 i18n + 3 处渲染层（含上游新桌面通知 #3715 的标题），**刻意保留** `BACKGROUND_BLOCK_START/END`（写进用户已存主题 CSS 的分隔标记，改名会让存量背景失效）。**顺带揪出**：`SessionCenter` 漏传上游新必填 prop（tsc 报错）、**fork 全部 6 个自有顶层路由从未接进反馈模块映射**（`/super-assistant`、`/enterprise/*`、`/skills`、`/mcp`、`/sessions`、`/memory`，用户在这些页面点"报告问题"预选不到模块，被上游新增的路由覆盖测试一次性暴露）。**③迁移校验和不再受行尾影响（真机抓到，而且我第一版结论方向是反的）**：CDP 冒烟第一次启动就 `BOOTSTRAP_DATA_INIT_FAILED / migration 19 was previously applied but has been modified`。sqlx 用迁移文件原始字节做 sha384，而行尾取决于检出环境。**实测打包源本身就是混合的：30 个 CRLF + 10 个 LF**，已发布版本写进用户库的就是这个混合校验和——所以单纯给 `*.sql` 钉 `eol=lf` 会把那 30 个哈希全改掉，**亲手触发**本要防的「所有存量用户升级后起不来」。正确修法是 `aionui-db/database.rs` 新增 `align_line_ending_only_checksums()`：`VersionMismatch` 时只在「去掉 \r 后文本完全一致」时对齐校验和并重试，真实改动照常拒绝（照抄既有 `align_reconciled_mcp_migration_checksum` 模式，2 条测试锁死）。真机验证：本机那个原本起不来的 dev 库自动对齐 `[19,20,21,25,31-36]` 后正常启动、40 条全匹配。**④顺带修的既有 Windows 缺陷**：`relative_path` 用平台分隔符导致前端（恒按 `/` split）文件树层级塌成一行；`replace_existing_path` 对 Windows 目录型 reparse point 用 `remove_file` 报 os error 5（悬空 junction 永远删不掉）；Linux 文件管理器 URI 兜底分支不做百分号编码；`aionui-project` 上游新 crate 的测试用 Unix 路径字面量在 Windows 上 17/19 从未跑通。**真机 CDP 已验证**：真实对话跑通（Start 7ms→Finish 1896ms，落库成功）、四 tab 合成、#3696 两处移植、#670 懒加载（队长 139ms / 成员 Dormant）。**⑤ `aionui-extension` 的 Windows 欠账已全部修完（12 条，不是初稿写的 5 条——`cargo test` 默认 fail-fast，lib 目标一挂就不跑集成测试，统计失败数必须带 `--no-fail-fast`）**：11 条同源于**扩展生命周期钩子在 Windows 上从来没能运行**（直接 spawn 脚本靠 shebang 找解释器，实测 `CreateProcess` 拒绝 `.sh`，`EFTYPE`/errno -4028），已改按扩展名分发解释器（`.sh`→POSIX shell / `.ps1`→PowerShell -File / 其余照旧），顺带解掉 Unix 上压缩包分发丢可执行位的隐患；第 12 条是悬空链接导入——**上一版声称"已修第一处"的那个修复其实是死代码**：它写 `if metadata.is_dir()`，而实测悬空 junction 是 `is_symlink=true / is_dir=false`（Rust 在 Windows 上对**任何**链接 `is_dir()` 都为 false），分支从不触发。**教训：平台特有分支的判断条件本身也必须实测——它可以完全合理却永远为假，而测试只会说"还是坏的"，不会说"你的修复根本没跑"。** 另外那两个 timeout 测试原本绕开 `execute_hook` 自己 timeout 一个裸 Command，既没断言本 crate 行为也跳过了解释器分发，已新增 `execute_hook_with_timeout` 改为驱动真实路径。18 个测试二进制全绿。**⚠️ 推送顺序不可颠倒**：先推 aionrs → 删 1oneCore `Cargo.toml` 末尾的临时 `[patch]`（指向本地 `../aionrs-local`）→ `cargo update -p aion-mcp` → 再推 1oneCore → 最后 1oneUI。同步全程用**仓库外 worktree** 隔离（另一条工作线有未提交的 P0-3 LanceDB + License Key 改动），⚠️ worktree 不能放仓库内部（`.git` 是文件，Vite 工作区根探测会窜到父仓库，180 个 dom 套件假失败）：
  [`docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md`](docs/guides/session-2026-07-29-upstream-sync-v028-v0153-v2143.zh-CN.md)
- **【2026-07-30】企业版商业化 5 项待办全量落地 + 知识库改混合检索 + 打包阻塞修复**：把 [`enterprise-commercialization-backlog.zh-CN.md`](docs/guides/enterprise-commercialization-backlog.zh-CN.md) §2 的 P0-1 续/P0-2/P0-3/P1-1/P1-2 全部做完。**⚠️ 该活文档两处结论已作废**：①P0-3 的「移植 AnythingLLM UI + 换 LanceDB 内核」——LanceDB 实测让 `aioncore.exe` 从 94.3MB 涨到 **299.3MB**（+205MB，含完整 arrow+datafusion）且 CI 需额外装 `protoc`，用户看数字后改选 **SQLite 内置 FTS5**（BM25 词法 + 向量 cosine，RRF 融合，trigram 分词支持中文），体积只 +3.1MB；②P1-2 的「统一 `created_by`」是错的，实际两套字段且五张看板表的冗余 `creator_name` 必须同改（否则界面仍显示离职者姓名）。**知识库缺口本质已闭合**：新增内置 MCP 工具 `search_team_knowledge`（照 export-pdf 的主进程 TCP + stdio 套路），三种 agent 后端通吃，ACL 自动复用；客户端模式下知识库在远端而主进程读不到渲染层会话，由新 hook 主动下发端点。**⚠️ 顺带修掉一个会让 `dist:win` 直接失败的既有遗留**：07-29 上游同步后后端改产 `schemaVersion: 2` manifest（agent CLI 从 npm 包换成原生二进制），但 JS 校验器与**装机侧 PS1 校验器**都写死只认 v1（后者会让已装用户被误判成"安装损坏"），两处均已支持 v1+v2。**真机 CDP 验证**：混合检索/RRF 确实纠正了纯向量的错误排序、MCP 工具走真实 TCP 协议、升档被 409 拦死、档位下拉只列≤当前档、备份往返幂等且脱敏精准（真实飞书配置 appSecret 被剥而 appId/redirectUri 保留）、移除成员全链路（JWT 指纹变化证明会话失效 + 审计归因操作者）、个人版红线。**过程中修了两个自己写出的 BUG**（Layout 崩溃、删文档不清词法索引导致正文永久留盘）。**⚠️ 同日后半段（用户追问五个产品问题后，范围继续扩大，见文档 §3-2/§3-3/§7.1）**：①**修掉两个越权**——判据是「守卫必须匹配数据作用域」：备份端点是我自己写出来的越权（挂 `RequireOrgAdmin`，但导出的是**整个部署所有租户**，任何组管理员都能导出别的组数据，已收紧 `RequireSystemAdmin`）；计费端点是**既有**越权（`org_admin` 能改整个公司的档位/席位/成本上限，已收紧）；1oneCore `985f86a9` / 1oneUI `d696207ee`。②**企业版信息架构重构**（用户原话「UI 设计不合理，缺乏美学」「不要修修补补，想重构都可以」）——项目组顶层页签 13→**6**，撑破视口把审计表列推出可视区的问题根治；按同一把作用域尺子把订阅与用量、备份与恢复迁进企业管理后台，其余按语义收进二级页签（**功能面一个没删**），旧深链保留别名映射+跨页重定向；1oneUI `2b1f54a5a`。真机 CDP 全绿（6 页签/无横向溢出/三组二级/四个旧别名各自落位/企业后台 6 页签）。⚠️ 验证方法自身有坑：只改 hash query 不会重新挂载组件而 `activeTab` 是 `useState` 初始化的，**必须整页 reload 逐个验**。③**§7.1 已解除**——另一会话收尾上游同步时把 `sync-v0153` 合进了主干，两仓 7 个提交现均在各自 `origin/one-main`，功能已对齐。**⚠️ 同日更晚：用户追问「改动同步到网页端了吗？之前好几次本地生效、网页端还是旧的」——问题真实且是结构性的（见文档 §3-4）**：`electron-vite dev` 只重编 `out/main`/`out/preload`，**从不碰 `out/renderer`**，而桌面应用把内嵌 WebUI 的 `staticDir` 指向后者；实测 `out/main`=19:34 而 `out/renderer`=**9:44（十小时前）**，产物里还留着旧文案、本轮新增字符串命中 0；全新 worktree 里 `out/renderer` 干脆不存在 → **`:25809/` 直接 404 而 `/api/*` 正常 200**。应用**自己在界面上打印那个地址**，所以这是陷阱不是操作失误。修法=`StaticServerOptions` 加可选 `rendererDevServerUrl`，dev 下两个桌面调用点传 `ELECTRON_RENDERER_URL` 把 renderer 请求反代到 dev server；**刻意不做「代理失败回落 staticDir」**（静默回落到旧产物正是要根除的），失败返回点名 dev server 的 502；HMR 的 WS 不经这层，故 dev 下 WebUI 是刷新即最新而非热更新。真机验证：`out/renderer` 仍不存在的前提下 `:25809/` 404→200、与 `:5173` 逐字节相同、含 `@vite/client`，透过它取到的源码含本轮的 `LEGACY_TAB_ALIASES`/`personalWorkspace`/`menuHeaderName`；4 条新单测。**同批还重构了侧栏工作区身份下拉**（用户截图指「重叠在一起、眼花缭乱」，上一轮只改了页签没动它）：同屏三次项目组名，根因是无 SSO 会话时 `displayName` 回落到 `context.tenantName`——把项目组名放进了「你是谁」的位置；已拆成身份/工作区两个变量、弹层标题去掉头像（与触发器重复）、留 8px 间距、「项目组设置 / 退出项目组」拆成单一标签、动作加图标与分隔线。1oneUI `490f784e8`。⚠️ **另一会话对 `pages/enterprise/index.tsx` 有未提交改动，与本轮 IA 重构同文件，合并须以分组结构为基线叠加**。License 公私钥轮换仍需人工离线执行：
  [`docs/guides/session-2026-07-30-enterprise-backlog-and-hybrid-rag.zh-CN.md`](docs/guides/session-2026-07-30-enterprise-backlog-and-hybrid-rag.zh-CN.md)
- **【2026-07-29】专家市场功能审查 + MCP 默认分发（ftshare/stock-sdk 加入内置默认列表）+ aionrs 单工具级 schema 过滤修复**：承接 07-28 那批新功能,用户问三件事——①有没有调用链 BUG（找到 Guid 页"+"菜单专家选择器安装失败时缺 `Message.error` 提示的轻微不一致,未修,记录见文档）②装进"我的助手"的专家为什么好像不能编辑（核实为非 BUG——`imported` 与 `user` 权限完全一致,"设置"菜单项就是编辑入口,只是文案不叫"编辑"）③打包会不会带上专家和 MCP（252 条专家人设编译期打进二进制,内置 MCP 随 asarUnpack 分发,均已确认）。**用户追加**：ftshare/stock-sdk 是股票专家查股票用的 MCP,确认没存密钥,能不能推给终端用户——真机 CDP 核实两者确实零密钥（`stock-sdk` 是 `npx -y stock-sdk-mcp` 公开包、`ftshare` 是纯网关 URL 无 headers）,已在 `buildDefaultMcpServers()` 照抄 `chrome-devtools` 的写法各加一条默认种子（未提交）。**用户追加**：已知的 ftshare "工具名不合法"07-27 bug 能不能修——真机核实上游 `market.ft.tech` 这个 schema bug 本身没修,只是 07-27 加的防线（整 server 排除）让用户感知不到崩溃；深挖发现 ACP 路径（Claude Code/Codex CLI）架构上真的做不到单工具级过滤（工具发现在 CLI 子进程内部,1oneCore 管不到）,但 **aionrs 路径不同——aionrs 自己拥有 MCP 工具发现逻辑,是 fork 自己的代码,可以改且安全**；已在 aionrs 仓库 `aion-mcp/src/tool_proxy.rs` 新增单工具级 schema 过滤（172 个工具只丢那 1 个坏的,其余 171 个照常注册,不再整 server 连坐),9 条新增单测 + 既有 597 条测试全绿,已提交推送 `gaogg521/aionrs` master `347348f`；1oneCore `Cargo.lock` 已更新指向新 commit 并重编 `aioncore.exe` 换入本机开发环境（本轮结束前开发环境已不在跑,未做真机复测）。详见：
  [`docs/guides/session-2026-07-29-marketplace-audit-and-mcp-tool-filter.zh-CN.md`](docs/guides/session-2026-07-29-marketplace-audit-and-mcp-tool-filter.zh-CN.md)
- **【2026-07-28】专家市场（Expert Marketplace，架构推翻重做过一次）+ Guid 页"选后端"与"选人设"解耦**：用户想把 WorkBuddy 导出的 281 个专家人设（Claude Code sub-agent 格式 `.md`）接入本产品，且要求人设能跨 1ONE CLI/Claude Code/Codex CLI 通用。**第一版**（`import_personas` + `POST /api/assistants/import-personas`，按 id 幂等 upsert，`assistant_definitions.source` 新增 `'imported'`，迁移 034）把 281 个人设直接物化进 `assistant_definitions`——真机验证时用户看到"我的助手"从 5 条变成 286 条，当场推翻："不要都挤到我的助手里面，应该单独再建一个表和展示列，名字叫做'专家市场'"。**第二版（当前状态）**：新增完全独立于 `assistant_definitions` 的 `assistant_marketplace_personas` 表（迁移 035）+ `IAssistantMarketplaceRepository`，目录内容随二进制打包（`crates/aionui-app/assets/marketplace-personas/`，照抄 `builtin.rs` 的 `include_dir!` 套路，`crates/aionui-assistant/src/marketplace.rs` 新模块，启动时幂等物化——全新装机也能看到完整目录，不是本机独有数据），刻意不塞进已经 6479 行超标 6.5 倍的 `service.rs`；新路由 `GET /api/assistants/marketplace`（浏览）+ `POST .../{id}/install`（安装，直接复用已有的 `import_personas()`，不重新造轮子）。⚠️ **顺带修的遗留**：`AssistantSource` 枚举只有 3 变体，导入的人设在四处分散的字符串→枚举映射里被 collapse 成 `"user"`，加 `Imported` 变体后编译器揪出 10 处非穷尽 match 全部修正，`deletable` 字段同步修正。**前端**：设置页三标签"我的助手/官方/专家市场"，新 `ExpertMarketplaceGrid.tsx`（281 张卡片+搜索，源数据没有分类/标签字段所以没做分类 pill）+ 独立 `useMarketplacePersonas` hook；侧边栏 "AGENT助手" 改名"助手与专家"+图标换豆包头像（13 语言）；另外还有 Guid 页 `BackendSelectionArea` 拆分"选后端"与"选人设"（`useGuidAssistantSelection` 新增 `selectedBackendAgentId`，默认跟随人设、手动切换一次后解耦，此后切人设不再重置）。真机 CDP 全链路验证（原始 WebSocket 直连渲染进程 9230，chrome-devtools MCP 连的是独立浏览器看不到 Electron 窗口）：搜索过滤、点击安装后卡片变"已添加"+ 后端行数据核实、旧的 281 条误导入数据已用脚本清理干净、Guid 页解耦行为符合设计。1oneCore `ca401c68`→`19461d24`→`52a00adf` / 1oneUI `48e13b256`→`5749413d7`；已重编 release 内嵌验证，未打包。**⚠️ 同日再晚些时候（同一天，范围继续扩大，见文档新增四~七节）**：①数据源再换一批（281→252，`display_name`/`role_name`/`category`/真实头像齐全，迁移 036，`marketplace_install` 补装完即写真头像 `set_avatar_from_bytes`）；②Guid 页聊天输入框"+"菜单新增专家选择器（对标 WorkBuddy，新组件 `GuidExpertPickerGrid.tsx`，选中态 chip 与"+"/模型选择器同一行，经用户多轮反复最终定为单排布局）；③修复专家搜索框 IME 中文输入法闪退 bug（真因是内层 `Menu.SubMenu` 在合成输入期间被 Arco 判定为"该关闭"，改用 `compositionstart`/`compositionend` 监听而非 focus 快照来抑制误关闭，用户真机确认修复生效）；④修复"+"菜单和团队页（新建团队/添加成员）的专家搜索只能搜到已安装 35 条、搜不到完整 252 条市场目录的 bug，统一改为搜索时合并已安装+市场目录、点未安装条目自动安装并选中。1oneCore `bbdb0029` / 1oneUI `046997a75`；均已 `tsc`/`lint`/`check-i18n` 验证，详见文档：
  [`docs/guides/session-2026-07-28-persona-import-and-backend-decouple.zh-CN.md`](docs/guides/session-2026-07-28-persona-import-and-backend-decouple.zh-CN.md)
- **【2026-07-27】MCP 管理越界写用户真实 `~/.claude.json` 修复 + 一键导入解析 BUG 修复**：用户自写 MCP（ftshare 含非法参数名占位符 `（无业务参数）`）装上后每条消息 400 崩溃，追出两条独立缺陷链。**①配置隔离**：Claude 桥接 spawn agent 时把 `CLAUDE_CONFIG_DIR` 指向隔离目录，但 `ClaudeAdapter`（1oneCore `aionui-mcp/adapters/claude.rs`）的 CLI 调用完全没带这个变量，`claude mcp add-json -s user` 写的是**用户自己的**真实配置——不只是互相看不见，在 app 内装/删 MCP 会连带改坏用户自己的 Claude Code 安装。修复为读写刻意不对称：`detect_existing`（一键导入的数据源）仍读真实家目录、`install_server`/`remove_server` 只写隔离目录，隔离路径抽到 `aionui-common/agent_bridge.rs` 与 agent 工厂共用防漂移；新增 `claude_config_isolation.rs` 直接调真实 `claude` 二进制验证（不是 mock）。**②工具 schema 校验闸门**：新增 `aionui-common/tool_schema.rs` 按 `^[a-zA-Z0-9_.-]{1,64}$` 递归校验，连接测试标注不兼容工具（前端弹黄色警告而非绿色成功）+ `load_user_mcp_servers` 跳过带毒的整个 server（ACP 不下发单个工具，只能整 server 拦）。**③一键导入解析 BUG**（同一天用户顺手点一键导入又发现的）：`ClaudeAdapter::detect_existing` 靠解析 `claude mcp list` 纯文本猜命令边界，两个真 bug——`shell_split` 把 `\` 当 POSIX 转义符吃掉 Windows 路径分隔符（`cfg!(windows)` 门控修复）、`SPLITTABLE_STDIO_LAUNCHERS` 白名单太窄导致整条命令行塞进 `command`（改为叠加 `~/.claude.json` 结构化字段，按 server 名字匹配覆盖猜测结果，匹配不到静默回退不影响存在性/连通状态）。**④一键导入弹窗加单独勾选**（原来是 all-or-nothing）：`OneClickImportModal.tsx` 加 per-server Checkbox + 全选/取消全选，默认全选保留旧行为。真机 CDP 全链路验证：ftshare 172 工具精确标出 1 个、其余 171 不受影响；会话勾两个 MCP 发送 0 个 400；`codegraph`/`one-image-generation`/`one-web-tools` 三个此前解析坏的 server 重新导入后 command/args 结构正确、Windows 反斜杠完整保留；取消勾选一项后该项确认没落库；全程 `~/.claude.json` 的 `mcpServers` 字段逐字节未变。**⑤ Codex 同款隐患收尾**（用户明确要求"肯定要处理"）：`CodexAdapter` 没有 Claude 那种"配置分裂"（agent spawn 本就没隔离 Codex 的 MCP 配置，Codex 的 LLM-provider 桥接走 `CODEX_CONFIG`/`MODEL_PROVIDER`，与 `~/.codex/config.toml` 的 `mcp_servers` 表无关），唯一问题是写操作会碰用户真实配置；`install_server`/`remove_server` 无生产调用方，隔离不需要碰 agent spawn，不强制用户重新登录 Codex。真机测试当场揪出 codex CLI 不像 claude CLI 会自动建目录，硬失败"CODEX_HOME points to..., but that path does not exist"，补了 `ensure_isolated_home()`。新增 `codex_config_isolation.rs` 直接调真实 `codex` 二进制验证 `~/.codex/config.toml` 逐字节未变。⚠️ 过程中一度跑了完全无关的全量 `aionui-app` e2e（52+ 测试，机器负载高单条 60+ 秒）被用户当场叫停——这种量级的改动（隔离逻辑 + 一行接线）针对性单测 + 编译通过即可，不需要全量 e2e，记入教训。1oneCore `835af6da`（配置隔离+schema 闸门，另有一批未打 doc 索引的后续提交见文档）+ `a39d9954`（导入解析）+ `5a0f8e4c`（Codex 收尾）/1oneUI `1631471dd` + `ed7a69742`；未打包：
  [`docs/guides/session-2026-07-27-mcp-config-isolation-and-import-fixes.zh-CN.md`](docs/guides/session-2026-07-27-mcp-config-isolation-and-import-fixes.zh-CN.md)
- **【2026-07-27】项目组/企业身份 5 个体验问题修复 + 「更新日志」打通**：用户真机反馈。①**客户端切服务器无二次确认且切完地址被清空**——根因是角色与地址耦合在同一次写入（切服务器时用空串覆盖地址，`markDeploymentAsServer` 同病），已拆成 `persistDeploymentRole` / `persistDeploymentServerUrl` 两个独立写操作（服务器模式只是**忽略**地址而不擦除），并补 client→server 确认框（排在"已加入项目组不可切"拦截之后）。②**地址无历史记录**——新增 `webui.enterpriseServerUrlHistory`（最多 8 条、最近在前、存量当不可信输入规整），输入框换 `AutoComplete` + 历史快捷按钮 + 清空。③**「连接远端项目组服务器」搬到「企业身份」页**（它干的是浏览器 SSO 登录 = 身份维度；地址输入仍留项目组页，跨页处补了「前往填写」按钮，SSO 回跳目标同步改为 `/settings/enterprise-identity`）。④**已 SSO 登录的用户头像菜单仍显示"登录"并跳「登录您的账户」死路**——按"企业 ⊃ 项目组"改三态，已登录未入组只留「加入项目组」直达 `/settings/enterprise`，副标题也不再谎称"个人版 · 未登录"。⑤**「更新日志」点开空白**——根因是**这个 fork 压根没有 release notes 数据源**（electron-builder 只有构建期存在 notes 资源才往 `latest*.yml` 写 `releaseNotes`，手动检查的 `body` 读的也是同一字段），已改为 `release-distribute.yml` 把 GitHub Release 正文作为 `releases/{version}/release-notes.md` sidecar 一起上传（选 sidecar 而非构建期生成，是因为发布说明写在建 Release 时，且可事后单独重传修正）、客户端在 yml 缺字段时 best-effort 拉取该文件、弹窗兜底文案改为永远给「暂无更新说明 + 前往查看」而不是永久"获取中"。⚠️ ⑤要**下次打包 + 下次发布**才生效，已发布的 2.1.50 补传 sidecar 也只有升级后才读得到。**真机 CDP 冒烟顺带揪出一处状态语义 BUG 并已修**：改完兜底文案后真机弹的是「获取失败，可 前往查看」，但检查本身是成功的——`checkAvailable`/`manualReleaseInfoLoaded` 把"正文为空"直接判成 `failed`，已改成请求成功一律 `loaded`、空不空交给组件说「暂无更新说明」。**⑥ 更新日志改为从提交自动提取，⑦ 再升级为「一份 JSON、三处展示」打通官网**（均用户追加）：新增 `scripts/generate-release-notes.js`（`bun run release-notes`），取"上一个可达 `v*` tag → HEAD"的提交，只留 `feat`/`fix`/`perf`（实测 40 条提交里只有 14 条够格）。**用户随即指出官网 `D:\website\1onework` 也有独立维护的「更新内容」区**（`src/changelog.js`）——官网**不是 git 仓库**（部署是本机 `npm run build` + `D:\game\scripts\deploy-1onework-www.py`，Python/SSH），摸不到 GitHub Actions，所以官网同步只能是发布时本机手动跑一条命令，和既有"官网改完直接部署不用问"约定一致。权威格式升级为 `docs/release-notes/<version>.json`（形状对齐官网 `changelog.js` 现有条目 `{version,date,zh:[{t,d}],en:[{t,d}]}`，`.md` 纯文本覆盖废弃）——`generate-release-notes.js` 加 `--draft-json` 起草模式，`en` 留空即视为"还是草稿"自动退回 commit 生成，防止未润色文案被发出去；新建 `scripts/sync-changelog-to-site.js` 把 JSON prepend 进官网 `CHANGELOG` + 顺带改 `site.config.js` 版本号，同版本重复跑会拒绝。**用户又追加**：`CHANGELOG` 全量渲染进落地页会让首屏越滚越长——`AskUserQuestion` 三选一后选了"独立更新记录页"（仿站内已有的 `docs.html` 多页构建先例）：落地页 `renderChangelog()` 只留最新 2 版 + 「查看全部」链接，新建 `updates.html`/`src/updates.js` 渲染完整历史、语言切换共享 `www-lang`；真机复测揪出一个真 bug（独立页误用了落地页 section 的 i18n key 导致标题对不上）已改正。用法与四步发布顺序（起草→润色→桌面端/Release→官网同步→部署）见 `docs/release-notes/README.md`。tsc/lint/i18n 过、测试 156 条绿（含新建 `WorkspaceIdentityEntry.dom`），**六项桌面端真机 CDP 冒烟 + 官网侧 build/preview/chrome-devtools 实机核对全过**（含"切服务器后地址仍在"的读库实证、sync 脚本 dry-run/写入/拒绝重复的完整验证）；未打包、未真正跑过一次完整发布：
  [`docs/guides/session-2026-07-27-project-group-ux-and-release-notes.zh-CN.md`](docs/guides/session-2026-07-27-project-group-ux-and-release-notes.zh-CN.md)
- **【2026-07-24】安装器错误弹窗品牌名漏改修复 + 品牌复检铁律入库**:用户真机(360 拦截 `d3dcompiler_47.dll` 触发 E1031)截图暴露安装失败弹窗正文全是「AionUi 安装失败」。根因=`resources/windows/installer-messages.nsh` 等 NSIS/PS1 脚本里手写硬编码文案漏改(此前批量刷品牌只覆盖 i18n locales)。已把用户可见文案(installer-messages.nsh 47 处 + messages.yml + process-control/repair-heal/remove-registry 的 locker 显示名与 DetailPrint + support/_.ps1 报告对话框文案 + 冒烟测试断言 + TestShowcase 标题)全改为「One Work」,内部标识符/`AIONUI*MSG*_`宏/`$AionUi\*`变量/`AionUi.RestartManager`命名空间/版权头/exe/appId 一律保留。并把「品牌复检铁律」写进 upstream-sync-reference §5.5。⚠️ 遗留待用户定夺:`report-installer-failure.ps1` 里"发送失败报告→ https://github.com/iOfficeAI/AionUi/issues"指向上游仓,是否换成 fork 自己的支持渠道。真正装不上的根因是 360 误杀 DLL(需临时关杀软/加白名单,根治要给安装包做代码签名)。
- **【2026-07-23 最新】P0-2 通用 SSO 第一步：OIDC（打通 Okta / Azure AD / Google Workspace）**：现有 SSO 只支持国产 IdP（飞书/钉钉/企业微信/LDAP），补一个标准 **OIDC** provider（`one-sso/providers/oidc.rs`）一次打通三大西方主流 IdP（都说 OIDC）。套现有 provider 抽象（discovery `.well-known/openid-configuration` → 授权码流 → userinfo 端点读 claims），**无迁移**（provider 表 config 是 opaque JSON）。前端设置页加 OIDC 卡片 + 登录页加 OIDC 按钮。**安全边界 v1**：身份走 userinfo 端点（access_token 可信），未做 id_token 签名校验（JWKS，列后续硬化）。个人单机版零影响（SSO 全是管理员显式配置默认关，个人从不配→无按钮）。one-sso **53 单测全绿**（+7 wiremock OIDC）+ 前端 tsc/oxlint/check-i18n 过；真机 CDP+打包待做。交付方式=逐个做透（P0-2→P0-4→P0-3），SAML/SCIM 紧跟其后：
  [`docs/guides/session-2026-07-23-oidc-sso.zh-CN.md`](docs/guides/session-2026-07-23-oidc-sso.zh-CN.md)
- **【2026-07-23】多成员模型（方向 B / Phase 2）— 一人多项目组 + active-tenant 切换**：承接 Phase 1（企业三层）。正式放开组织模型地基——`one_user_org` 主键 `user_id` → 复合 `(user_id, tenant_id)`，引入**服务端按用户存的 active-tenant（当前作用项目组）**。关键设计=active-tenant 服务端存储让 `tenant_of(user_id)`/`effective_role(user_id)` **签名全不变**（`OrgActor`/rbac.rs 零改动、团队资源 scoping 自动跟随），切换 = `POST /api/one/org/switch` 服务端写、**不轮换 JWT**、下一请求即生效。改动：迁移 007（表重建复合 PK + `one_active_tenant` + 回填）；one-org 新 `active_tenant_id`/`list_memberships`/`set_active_tenant`、`join` 去总闸改「已在该组则拒绝」+ 设激活、`leave(user_id, tenantId?, code)` scoped 退组 + 退激活重选、`/my-tenants`+`/switch` 路由；三处跨 crate 直读角色（one-sso/one-enterprise/one-devops）改「按激活组解析」单条 SQL；前端 `useMyTenants` hook + 徽标下拉「我的项目组」切换器。**红线守住**（个人版无成员行→回落 default、`one_active_tenant` 恒空零写，锁死测试全绿含新增 `active_tenant_defaults_when_no_membership`）。后端单测全绿（one-org 28/one-sso 46/one-enterprise 12/one-devops 23）+ 前端 tsc/oxlint/check-i18n 过；**真机 CDP + 打包待做**：
  [`docs/guides/session-2026-07-23-multi-membership-phase2.zh-CN.md`](docs/guides/session-2026-07-23-multi-membership-phase2.zh-CN.md)
- **【2026-07-23】Codex + Claude 自定义模型桥接双双落地 + 模型选择器锁定 + 桥接注入机制根治**：为让 Codex CLI/Claude Code 都能用自定义 provider（不牺牲多模型能力、不需要 OpenAI/Anthropic 官方订阅），新增桥接服务 + 设置页 + 模型选择器锁定（桥接开启时桌面端/移动端都不可再手动切模型，避免 `session/set_model` 实时 RPC 绕过桥接）。**Codex 真机测试暴露真 bug 并已根治**：应用内置"Codex CLI"实际 spawn 的是随包分发、托管的 `@agentclientprotocol/codex-acp` Node 包装层，完全独立于用户系统装的 `codex` CLI 和 `~/.codex/config.toml`；该包装层自己的 argv 解析只认 `login`/`cli`/`--version`，此前用 `-c key=value` 注入桥接配置**全部被无声丢弃**（反解包实锤，此前从未真正生效），真正读取配置的方式是 `CODEX_CONFIG`（JSON）+ `MODEL_PROVIDER` 两个环境变量——已重写注入机制（1oneCore `acp_launch_policy.rs`），顺带发现并一并修复了同一套失效机制下的既有沙箱权限模式注入。**Claude 桥接**（`aionui-claude-bridge` 新 crate）比 Codex 简单——Claude Code 原生说 Anthropic 协议，不需要本地协议转换，直接把保存的 provider 解密成 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_MODEL` 三个环境变量注入，同时发现此前 Claude 的自定义模型能力完全依赖外部第三方工具「cc-switch」（本应用零 UI/写入路径），现在有了第一方设置页、外部工具仍保留作 fallback。两条桥接都经过**真机 CDP + 诊断日志双重验证**：Codex 靠 codex-acp 启动日志确认收到正确配置，Claude 靠真实发消息拿到模型回复；都不再报官方账号 401。**真机测试顺带揪出一个真 bug**：两个设置页保存后，已挂载的模型选择器读的是保存前的 SWR 缓存，不重启应用感知不到——已加 `mutate()` 手动失效修复。已知遗留（非 bug，纯展示）：Claude 会话头部/Guid 页显示的模型名仍来自未改动的 cc-switch 展示层，跟桥接实际生效的 provider/model 不一致，功能正确但文案对不上。**同日晚些时候两个真机复测跟进**：①反解包真实 `codex.exe` 二进制确认 `model_context_window` 是真实存在的顶层 config 字段，已从桥接 provider 的 `context_limit` 注入——真机确认这个数字真的被用于 Codex 实际的上下文预算计算（`modelContextWindow` 报值随之变化），但「Model metadata not found」这条 warning 本身没能消掉，因为同一份诊断日志里有独立的真实内部 ERROR（`codex_models_manager::cache` 解析失败）证明这是 Codex 二进制自己的模型元数据缓存 bug，不是本应用配置能左右的；②**发现并修复了 Guid 欢迎页（新建会话前）的模型选择器是完全独立于会话内 `AcpModelSelector` 的另一个组件，从未接入桥接锁定**——桥接开启时这里此前仍是可点开切换任意模型的下拉框，现已同步加锁，真机 CDP 确认 Codex/Claude 两个 tab 都不再可点开切换。**③同日再晚些时候：用户凭真机截图敏锐怀疑"Claude 桥接读的是我本地 `~/.claude/settings.json`"——怀疑完全正确，已根治（1oneCore `factory/acp.rs`）**：`CLAUDE_CONFIG_DIR` 隔离目录只挡住了 settings.json **文件**本身，反解包 SDK 自身字符串表确认 `ANTHROPIC_DEFAULT_HAIKU_MODEL`/`_SONNET_MODEL`/`_OPUS_MODEL`/`ANTHROPIC_SMALL_FAST_MODEL` 是独立于该文件之外、直接从进程环境变量读取的模型别名，而这几个变量在这台机器的进程树里本来就是"环境态"存在（继承自操作者自己的真实 Claude Code 环境），托管 agent 子进程从不清理它们，导致会话自己的 `/model haiku` 启动引导解析出真实 settings.json 里配置的别名模型，令桥接实际转发的模型与用户在设置页选的完全不是一回事。已把这 4 个别名环境变量在 `resolve_claude_bridge_env()` 里全部钉死为桥接自己配置的模型。**真机双重验证**（读磁盘上的真实 session transcript 而非 UI 展示的模型名——后者走独立展示层，即上面提到的"纯展示不一致"已知项，不能作为判据）：修复前的诊断 transcript 能看到 `/model haiku` 解析出真实设置别名；修复后重新发消息，transcript 里既无该启动引导，也无真实 settings.json 任何字符串痕迹（0 匹配），响应 `model` 字段正确显示为桥接配置的模型本身。**④同日再晚些时候：§4.2/§5.3 遗留的"会话头部模型名展示滞后"用户真机复测再次点出，已修**——Codex 桥接改到 `glm-5-2-aliyun` 保存后，会话头部胶囊显示的仍是 ACP session spawn 时广播的旧快照名(功能路由正确,纯展示滞后)。根因=展示逻辑从 session 快照读名字,从不因桥接配置保存而重新拉取。修复=`useCodexBridgeStatus`/`useClaudeBridgeStatus` 改为缓存完整配置对象、新增 `useCodexBridgeModel`/`useClaudeBridgeModel`,`AcpModelSelector`/`GuidModelSelector` 锁定态优先用桥接自己的 `model` 而非 session 快照。真机验证:新会话与更早创建的历史会话,头部胶囊都统一显示当前桥接配置的模型:
  [`docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md`](docs/guides/session-2026-07-23-codex-bridge-and-injection-fix.zh-CN.md)
- **【2026-07-22~23】真实企业升级为治理层（方向 B / Phase 1）+ 企业客户端登录卡死/身份修复 + Windows 尾随空格 bug 修复 + 企业/团队路线图**：真实企业从纯身份标签升级为**项目组之上的治理层**（个人 ⊂ 项目组 ⊂ 企业）——公司显式设立、成员与角色、企业管理员**代建多个项目组**（用「代建空组、不动 `one_user_org` 主键」绕过一人一组约束）、SSO 配置迁入企业后台；**个人单机版零影响**（门控 + 锁死测试），**真机 CDP 全流程验证通过**。同会话早段修了①企业客户端登录后整个 UI 卡死（`RemoteServerSection` 把 `getEnterpriseSession()` 每渲染新对象当 effect 依赖→无限渲染→饿死路由 transition）②「已 SSO 登录却显示未登录」（`/api/one/enterprise/me` 依赖飞书 tenant_key，approach-B 回退显示会话身份）。顺带修了 07-18 上游带入的既有 Windows 尾随空格 workspace 校验 bug。**竞品对标 + P0/P1/P2 商业化路线图**（多成员/通用 SSO+SCIM/席位计费/细粒度 RBAC/agent 审计…）见：
  [`docs/guides/session-2026-07-22-company-tier-direction-b.zh-CN.md`](docs/guides/session-2026-07-22-company-tier-direction-b.zh-CN.md) ·
  [`docs/guides/enterprise-team-roadmap.zh-CN.md`](docs/guides/enterprise-team-roadmap.zh-CN.md)
- **【2026-07-21 最新】Mac 打包签名公证打通 + macOS 老系统 Node 兼容修复 + 发布链路排障**：Apple Developer 证书配好后签名一度假阳性成功（`IDENTITY` secret 多带了 `Developer ID Application:` 前缀导致签名失败，但 DMG 重试兜底逻辑吞掉了失败继续打出未签名包，CI 照样报绿）；用户真机（MacBook Pro M1 2020 / **macOS Big Sur 11.6**）装上后报"托管 Node 运行环境无法启动"，追到 [`aionui-runtime/node_runtime/mod.rs:228`](../1oneCore/crates/aionui-runtime/src/node_runtime/mod.rs) 真的在 spawn `node --version`，推断是内置 Node 24 用较新 Xcode 工具链的 chained-fixups 格式老系统 dyld 加载不了（公证两次都干净通过，排除签名问题）；已给 macOS 单独把托管 Node 降到 22.11.0（Windows/Linux 不变，1oneCore commit `1797fcf7`），**✅ 已在真机确认修复生效**（报错消失）。顺带切了正式版本号（1oneCore `v0.1.49-one.1` Release 已发布 6 资产齐全 / 1oneUI tag `v2.1.48`）、修了"下载最新版"按钮误跳上游官网（改指向 `work.1oneclaw.com`）、纠正了一次误诊（以为是 Apple 公证排队，真相是本地 `codesign` 进程挂死近 6 小时）。**⚠️ 发现 `Build and Release` 打 tag 自动发布流水线在这个 fork 历史上从未真正触发过**（`total_count: 0`），用户认领自查。**发布链路（Release/COS 上传）当前状态是活的、别在索引里另记一份快照**——权威记录 + 实时验证命令见 [`session-2026-07-21-brand-rename-and-release-fixes.zh-CN.md`](docs/guides/session-2026-07-21-brand-rename-and-release-fixes.zh-CN.md) §6：
  [`docs/guides/session-2026-07-21-mac-signing-and-node-compat.zh-CN.md`](docs/guides/session-2026-07-21-mac-signing-and-node-compat.zh-CN.md)
- **【2026-07-21】长 Write 工具调用被截断后静默丢失(用户抓包定位)+ kimi-k3 网关超时坑(已修复并真机验证)**：用户反馈长文件生成聊天里"写完了"但磁盘上没文件；搭本地转发代理在 dev 环境实测抓包，确认根因链：`openai_defaults()` 从不设 `default_max_tokens`→请求省略该字段→网关自己的默认上限（实测 4096）接管截断→截断落在 `Write` 工具调用中途时半截参数在 provider 层就被直接丢弃→引擎续写轮禁用工具，模型只能把没写完的内容当纯文本继续吐。修了两处（aionrs commit `33c2bd2`）：①`openai_defaults()` 补 `default_max_tokens: Some(32_000)`；②新增 `LlmEvent::ToolCallTruncated` 事件，`run_inner` 检测到工具调用被截断时不再走禁用工具续写，而是可见提示+保留工具重试。dev 环境重编 + CDP 实测 glm-5-2 验证生效（1560 行文件一次写成）；新增 wiremock e2e 测试锁死该路径。**⚠️验证中意外发现一个新坑**：kimi-k3 这类慢速重推理模型的超长单次请求现在有机会拖到 10 分钟量级，撞上网关自己的连接超时（EOF 断连，无 ERROR 日志、无可见报错，同样静默失败）。**✅ 同日已修复并真机验证**（aionrs `45cce3a`，OpenAI 兼容协议路径）：`process_openai_sse_stream` 改为 EOF 未见 `[DONE]` 时返回可见错误；1oneCore 重编 + CDP 直连渲染进程发真实 kimi-k3 请求实测，9 分 55 秒后真实复现原问题场景（`duration_ms:595137`/`termination:"eof"`/`done_seen:false`），UI 上确认弹出可见错误卡片（错误码 `UNKNOWN_UPSTREAM_ERROR`，技术详情含修复代码里的错误文案），不再静默"finished"。**⚠️范围边界**：这次修复只覆盖 OpenAI 兼容协议路径（kimi-k3/glm-5-2 复现时走的都是同一个 `litellm-internal.123u.com` 网关的 OpenAI 兼容协议）；Anthropic 原生协议路径（`anthropic_shared.rs`，走 `/v1/messages`）截断机制不同——`content_block_stop` 会把半截 `input` 坍缩成 `{}` 当正常 `ToolUse` 发出，不是直接丢弃，是另一条相关但不同的缺口。**同日晚些时候两次跟进已全部闭合**：aionrs `34f827b` 补"连接中途断连没走到 `message_stop` 就静默报成功"、aionrs `d309fb5` 补 `content_block_stop` 坍缩成 `{}`(改为区分合法空参 vs 被截断→发 `ToolCallTruncated` 复用 `33c2bd2` 的可见提示+保留工具重试机制,含真实 `/v1/messages` wiremock e2e);至此 Anthropic 原生协议两半截断缺口全部闭合,直连 Anthropic 官方 API 也不再有此坑:[`docs/guides/session-2026-07-21-truncated-tool-call-recovery.zh-CN.md`](docs/guides/session-2026-07-21-truncated-tool-call-recovery.zh-CN.md)
- **【2026-07-21】品牌名统一为「One Work」+ 设置页滚动修复 + 截断续写实测 + `release-distribute.yml` 三个真 bug 修复**：①**品牌**:显示名与打包名统一改为 **One Work**(此前显示「1One Work」/ 打包「1ONE Code」不一致);改 electron-builder `productName`/4 处 `artifactName`(→`One-Work-*`)/`package.json productName` + 界面文案 `1One Work`→`One Work`(825 处/115 文件,含 13 语言 i18n);**关键防坑**:生产环境新增 `app.setName('1ONE Code')` 钉子(`configureChromium.ts`/`platform/index.ts` 的 `PROD_USERDATA_APP_NAME`),让 userData 目录仍在历史 `%APPDATA%\1ONE Code`,**否则改 productName 会导致老用户会话/模型 Key 全丢**(打包版实测钉子生效);exe(`1onecode.exe`)/appId(`com.huanle.oneone.ai`)不动;**品牌红线现在守 One Work**。②截断续写(aionrs `b2b7bde`/12 轮)已 dev 真机实测两轮:一次 12 轮内收尾无横幅、一次耗尽 12 轮弹**新文案**横幅(旧「was cut off」已根除);⚠️新横幅建议「raise max output tokens」指向已删的设置项,属误导待议。③打包坑:`dist:win` 必设 `AIONUI_BACKEND_LOCAL_PATH` 用本地 aioncore,已封装 `scripts/package-win.ps1`。④**`release-distribute.yml` 首次真跑就暴露 3 个从未被发现的真 bug 并修复**(元数据校验硬编码全平台放宽为按实际存在校验、COS 拒绝 path-style 需强制 virtual 寻址、COS 大文件分片上传缺 Content-Length 需抬高 multipart 阈值走单次 PUT),另加 `force` 选项支持重试;`v2.1.48` GitHub Release 已建且带 `One-Work-2.1.48-win-x64.exe` + `latest.yml` 两个资产。CI 上传在 GitHub Actions 侧卡住 2 小时+ 被取消,已改本地手动上传(`aws-cli` + Tencent COS 凭据)并 `curl` 验证成功——**✅ v2.1.48 已正式发布,公开可下载**;⚠️该版本不含同日晚些时候另一会话落地的长 Write 截断恢复修复(`700e7f75`),留到下一版本号,详见该文档 §6.3/§8:
  [`docs/guides/session-2026-07-21-brand-rename-and-release-fixes.zh-CN.md`](docs/guides/session-2026-07-21-brand-rename-and-release-fixes.zh-CN.md)
- **【2026-07-20 最新】aionrs 输出截断续写修复 + 三仓上游二次同步（07-20 批次：aionrs v0.2.6 / Core 9 提交 / UI 5 提交）**：用户反馈长代码生成被截断，根因是 aionrs 撞输出上限后只补救一轮，真正长内容必然再次撞上限就直接放弃；已改成最多 12 轮有界续写并逐段拼接（`aionrs` commit `9fa951e`），新增真实 HTTP+SSE 的 3000 行复现测试。顺带把三仓补到当天最新上游：**同步原则改为默认信上游、只守图标+"1One Work"品牌文案两条红线**；1oneCore 按上游 #641 撤销了 fork 自建的"按模型最大输出 Token"功能分支（⚠️ 设置页那个输入框现在是死 UI，见文档 §5）；1oneUI 合并时发现并撤销了对已删除的桌面宠物子系统（07-07 `33f8aae28`）的意外复活。**验证中意外揪出两个真实 bug**：早前品牌重写脚本把内部错误码标识符（`AIONUI_CONVERSATION_BUSY` 等 6 个 key）当文案一起替换成"1One Work\_\*"，导致 5 种真实错误状态在全部 13 语言下都显示不出文案，已在全部 locale 文件恢复正确 key（只改 key 不改翻译）；自动更新 CDN 测试断言自 07-16 改指向自建 COS 后就没跟上，已同步。三仓测试：aionrs cargo test 全绿；1oneCore 唯一失败是已记录的既有 Windows 路径 bug；1oneUI 310/312 测试文件通过（2 个已知不修项已记录）：
  [`docs/guides/session-2026-07-20-truncation-fix-and-upstream-resync.zh-CN.md`](docs/guides/session-2026-07-20-truncation-fix-and-upstream-resync.zh-CN.md)
- **【2026-07-20】企业组织 vs 项目组彻底解耦落地（含三轮：架构重构 + 用户真机点出3个问题 + 全局测试）**：新增独立 `one-enterprise` crate（`one_enterprises`/`one_enterprise_members`）承载 SSO 公司维度，`one-org` 的 `one_tenants` 剥离 `sso_provider`/`sso_org_id` 两列回归纯邀请码项目组；此前 07-17 中断的 stash（`wip-enterprise-before-sync-v0148`）已续完并落地，**stash 可以清了**；设置菜单拆成「项目组」+「企业身份」两个独立页面/菜单项（`/settings/enterprise-identity`）；**⚠️ 修复真 bug**：`httpBridge.ts` 的 `getBaseUrl()` 无路径判断，连接远端项目组服务器后把文件上传/语音转文字/目录浏览/微信登录二维码/Office预览/**全站图标资源**这 6 处本该恒本地的调用点全部指向远端导致卡死，已改 `getLocalBaseUrl()`；顺带修 6 个 M4d（Bearer 豁免 CSRF）之后过期的测试断言 + `memory` 页面表格列 duplicate-key 告警。**`cargo test --workspace` 发现 1 个未修的既有 Windows 平台 bug**（`aionui-common::validate_workspace_path_availability` 对尾随空格路径的校验在 Windows 上因 Win32 API 静默剥空格而失效，07-18 上游同步带入，与本轮无关，留下一轮修）：
  [`docs/guides/session-2026-07-20-enterprise-org-decouple.zh-CN.md`](docs/guides/session-2026-07-20-enterprise-org-decouple.zh-CN.md)
- **【2026-07-20】三仓上游同步（v2.1.37/Core 0.1.48/aionrs 0.2.5）合并后 BUG/冲突静态审查**（16 项核查，无阻断发布的真实 BUG；发现品牌注入（`9504fa47`）遗漏 4 个文件 ~12 处 agent 会读回的 "AionUi" 错误字符串待补齐；看图白名单归一化启发式对 `minimax`/`deepseek-v4-flash` 形似品牌名有误切缺陷（当前无真实碰撞，建议补测试锁死）；`wip-enterprise-before-sync-v0148` stash 澄清为完全独立、从未落地的休眠重构方案，round25 生产代码原样健在，不是"丢失的修复"；侧栏拖拽排序与弹性历史交互仅静态核查，真机行为待验证。**本轮只做了静态代码审查，未做运行时/CDP 测试**）：
  [`docs/guides/session-2026-07-20-post-sync-bug-conflict-audit.zh-CN.md`](docs/guides/session-2026-07-20-post-sync-bug-conflict-audit.zh-CN.md)
- **【2026-07-16】飞书桌面登录根治 + 「真实企业」层(B2)**（一条从表象追到架构的链：先排除 07-15 的 scheme 抢注（注册表/合成 deep link/抓包三重实证客户端全对），**真因是回调页 `desktop_callback_page` 里 `setTimeout(window.close, 1200)` 抢在浏览器"要打开 Electron 吗?"确认框之前把标签关了**——与 scheme/平台无关，此前所有平台桌面 SSO 登录都走不完最后一步，已改 5 秒+醒目按钮；顺带修「显示 `sso_019f68e0` 而非赵高」= 中文用户名被 sanitize 掉 + deep link 没传 display_name；再引出架构盘点：**SSO 认证 ≠ 加入组织**，飞书 `tenant_key`(公司标识)此前被丢弃 → 新增「真实企业」层：同公司 SSO 登录自动入伙。**⚠️红线：`auto_provision_enterprise` 是 join-only、永不建 tenant，`auto_joiner` 为 Option，单机版行为零变化并有测试锁死**；含小猫图标、自动升级改指向自有 COS、企业→项目组改名。后端已完成+测试通过，前端待做，未提交未打包）：
  [`docs/guides/session-2026-07-16-enterprise-tier-and-sso-fixes.zh-CN.md`](docs/guides/session-2026-07-16-enterprise-tier-and-sso-fixes.zh-CN.md)
- **【2026-07-15】历史会话引用已删除 Provider 时报裸内部错误，加友好提示**（`Provider 'xxx' not found` 原样透传给用户；根因是用户删除/重建过模型配置，老会话的 `model` 字段还固化着旧 provider ID；新增 `AgentErrorCode::ProviderNotFound` + 13 语言 i18n；**关键教训**：最初以为走的是 `sendMessage` 同步 HTTP 报错路径，改完单测全过，CDP 实测发一条真消息才发现实际命中的是异步的 WebSocket 流式协议路径——完全是另一套分类逻辑，及时补上了真正的分支）：
  [`docs/guides/session-2026-07-15-provider-not-found-friendly-error.zh-CN.md`](docs/guides/session-2026-07-15-provider-not-found-friendly-error.zh-CN.md)
- **【2026-07-15】Mac 打包（GitHub Actions 无本地 Mac 机器）已跑通并真机验证**（`macos-14` runner，public 仓库免费；真坑不是 macOS/CI，是 1oneCore 后端版本号在代码里改了却从没打 tag 发 Release，1oneUI 下载 aioncore 二进制在 `Prepare aioncore binary` 步骤 404；解法是去 1oneCore 补打对应 tag 推送触发它自带的跨平台 release 流水线；readme 补上了 unsigned 包的 Gatekeeper 拦截绕过说明，未来配了签名证书要记得删掉这段）：
  [`docs/guides/session-2026-07-15-mac-packaging-github-actions.zh-CN.md`](docs/guides/session-2026-07-15-mac-packaging-github-actions.zh-CN.md)
- **【2026-07-15】企业 SSO 桌面 deep link 被 dev/打包版互相抢注根治**（浏览器显示"登录成功"，桌面端却仍是访客模式；根因是 `aionui://` 协议在 Windows/macOS 是全局唯一注册，dev 模式和打包版共用同一个硬编码 scheme，谁最后启动谁抢走注册表 key；改成 `app.isPackaged ? 'aionui' : 'aionui-dev'` 各注册各的，后端 `/authorize` 新增 `scheme` 查询参数走闭集合白名单校验（防 JS 注入）；registry 实测 + CDP 合成 deep link 双重验证根因与修复）：
  [`docs/guides/session-2026-07-15-sso-deep-link-scheme-collision.zh-CN.md`](docs/guides/session-2026-07-15-sso-deep-link-scheme-collision.zh-CN.md)
- **【2026-07-14】SSO 设置表单非密钥字段不回显 BUG 修复 + 管理端点补角色校验**（公开状态接口把整个 config 都剥了导致管理页 App ID/Redirect URI 等非敏感字段也一起被藏；新增管理员专属只读端点只剥密钥字段；顺带发现 `one_sso_admin_routes` 缺管理员角色门控——任何登录成员可改 SSO 配置甚至借道伪造 LDAP 服务器做账号劫持——已原地修复；两轮均 CDP 实测）：
  [`docs/guides/session-2026-07-14-sso-settings-prefill.zh-CN.md`](docs/guides/session-2026-07-14-sso-settings-prefill.zh-CN.md)
- **【2026-07-13】企业「本机作为客户端」页 6 个 BUG 修复**（地址重复填/加入成功却报错/远端数据文案撒谎/客户端看不到成员邀请码/SSO 保存被整体覆盖/后台 URL 弹到 guid；含 local-first 路由前提、按键合并、只读成员端点、CDP 实测结论）：
  [`docs/guides/session-2026-07-13-enterprise-client-6bugs.zh-CN.md`](docs/guides/session-2026-07-13-enterprise-client-6bugs.zh-CN.md)
- **【2026-07-13】Agent 助手反复失败根治**(deferred 工具 stub schema 在 GLM 受约束解码下永远空参→熔断→误报 UNKNOWN_UPSTREAM_ERROR;助手技能从未接进 aionrs;四根因一次修完,含会话取证/黑盒探测/日志判别式方法论)：
  [`docs/guides/session-2026-07-13-deferred-schema-and-assistant-skills.zh-CN.md`](docs/guides/session-2026-07-13-deferred-schema-and-assistant-skills.zh-CN.md)
- **Agent 思考模型报错 / 网关拒绝 tool_calls / 授权模式默认全自动**（2026-07-10~11，本轮最重要）：
  [`docs/guides/session-2026-07-10-thinking-param-and-rename.zh-CN.md`](docs/guides/session-2026-07-10-thinking-param-and-rename.zh-CN.md)
  - **问题①②③ 思考模型报 `content[].thinking must be passed back`**：黑盒探测定位为网关（`litellm-internal.123u.com`）DeepSeek 渠道**无状态地拒绝一切含 assistant `tool_calls` 的历史**（与 thinking 声明/格式无关，glm/claude 渠道正常）。aionrs fork 用**「文本化工具历史」**兜过（把历史 tool_calls 降级成纯文本），会话内粘住已学等级。commit `1f36350`。
  - **授权模式默认「全自动」**（aionrs=`yolo` / ACP=`bypassPermissions`）：改在 Guid 页 `useGuidAssistantSelection` + `pickFullAutoMode`。commit `e4f56ac`。
  - 还含：**黑盒探测网关方法论**（解密 provider api_key 直接 curl 构造变体）、**上游对齐**（iOfficeAI/aionrs issue #74 / PR #203：thinking 只在显式请求时声明）、**三仓 commit 索引**、诊断踩坑（dev userData 是 `%APPDATA%\1one-Dev`、`aion_providers` 日志被封顶要用 warn 穿透、legacy bridge helper 是死代码 等）。
- **前后端加载约定**（改完必须重编哪个才生效）：[`docs/guides/ai-handoff-conventions.zh-CN.md`](docs/guides/ai-handoff-conventions.zh-CN.md)
- **CDP 调试**（比 computer-use 坐标点击可靠）：[`docs/guides/cdp.md`](docs/guides/cdp.md)
- **Fork 上手 / 本地 dev / 打包 Release**：[`docs/guides/fork-dev-onboarding.zh-CN.md`](docs/guides/fork-dev-onboarding.zh-CN.md)
- **【2026-07-11~12 最新交接·先读】三仓上游同步已全部完成并正式发布**(前端 v2.1.32 / 后端 v0.1.45 / aionrs v0.2.2 均合主分支推 origin;出 2.1.37 安装包;含发布连锁铁律、dev 活测机制、migration/Cargo.lock/打包各坑)：[`docs/guides/session-2026-07-11-upstream-sync-v2132-handoff.zh-CN.md`](docs/guides/session-2026-07-11-upstream-sync-v2132-handoff.zh-CN.md)
- **上游对照与同步参考**（前端=AionUi / 后端=AionCore / aionrs 的仓库映射、版本对照、当前同步状态、同步套路与不变量、出问题去上游哪里找）：[`docs/guides/upstream-sync-reference.zh-CN.md`](docs/guides/upstream-sync-reference.zh-CN.md)
- 全部 session 文档索引见 [`docs/README.md`](docs/README.md) 的 **Quick pointers**。
